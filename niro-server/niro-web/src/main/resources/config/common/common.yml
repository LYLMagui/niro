
# 爬虫相关配置
spider:
  log:
    # 爬虫日志路径，优先使用环境变量 SPIDER_LOG_PATH
    # 开发环境默认指向 ../../niro-spider/logs/niro_spider.log (相对于 niro-web 启动目录)
    path: ${SPIDER_LOG_PATH:../../niro-spider/logs/niro_spider.log}
