/**
 * 全局业务常量
 * @author liyl
 * @date 2026-01-08
 */

/**
 * 系统管理员默认ID（临时方案，后续应从 UserStore 获取）
 */
export const ADMIN_USER_ID = 1;

/**
 * 默认分页大小
 */
export const DEFAULT_PAGE_SIZE = 10;

/**
 * 默认分页选项
 */
export const PAGE_SIZE_OPTIONS = ["10", "20", "50", "100"];
