# 订单记录页索引

## 主题
订单记录页交互与页面结构调整。

## 当前状态
设计中：订单记录页账号维度改造已确认，待实现。

## 当前有效结论
- 订单记录页已完成与 C5 任务页标准对齐，并已支持同步范围选择。
- 当前新增账号维度改造：手动同步 C5 订单时必须选择单个 C5 账号，后端按所选账号 AppKey 同步并写入 `accountId`。
- 表格账号筛选默认为空；为空时后端返回当前用户全部账号订单，只有选择账号时才按 `accountId` 过滤。
- 表格“平台/账号”列需要按平台回显账号名称：C5 查 `c5_sniping_account`，BUFF 查 `buff_account`。

## 文档清单
- `按 C5 任务页标准对齐.md`
- `同步范围选择.md`
- `账号维度改造.md`
- `变更记录.md`

## 推荐阅读顺序
1. 当前账号维度改造实现前：先读 `账号维度改造.md`。
2. 需要追溯同步范围交互时：阅读 `同步范围选择.md`。
3. 需要追溯页面结构对齐原因时：阅读 `按 C5 任务页标准对齐.md`。
4. 需要查看演进时间线时：阅读 `变更记录.md`。

## 已完成项
- [x] 订单记录页按 C5 任务页标准对齐
- [x] 订单记录页同步范围选择设计

## 待实现项
- [ ] 订单记录页账号维度改造
