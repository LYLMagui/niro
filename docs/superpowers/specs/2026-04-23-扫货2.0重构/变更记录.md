# 扫货 2.0 重构变更记录

- 2026-04-23：建立“扫货 2.0 重构”主题目录，确认采用目录化规格。
- 2026-04-23：确认 2.0 首版仅面向 C5，上线策略为“C5 先上线，模型按多平台预留”。
- 2026-04-23：确认 2.0 只重建任务域，复用账号、商品、订单主数据与底层交易能力。
- 2026-04-23：确认数据库会清空初始化，旧 1.0 任务不参与并行运行，旧实现只保留源码。
- 2026-04-23：确认任务模型采用“单商品单任务”，运行模型只保留持续扫描。
- 2026-04-23：确认停止条件支持“按购买数量停止”与“按余额停止”，余额停止同时支持 `maxPrice` 与 `reserveBalance` 两种口径。
- 2026-04-23：确认账号关系为“每任务固定绑定 1 个账号；一个账号可绑定多个任务”。
- 2026-04-23：确认账号维度存在并发上限 `N`，调度策略采用“优先级优先，同优先级轮询”。
- 2026-04-23：确认同账号同商品只允许 1 个启用任务。
- 2026-04-23：确认命中条件只支持价格与磨损，不支持其他饰品属性过滤。
- 2026-04-23：确认幂等保护采用“内存短期去重 + 数据库唯一约束”的双层方案。
- 2026-04-23：确认任务表与运行实例表拆分，并记录命中明细与下单尝试明细。
- 2026-04-23：确认异常采用有限重试，人工介入仅支持任务级启停。
- 2026-04-23：确认列表口径为“任务汇总 + 最近一次运行信息”，扫描频率采用“全局默认 + 任务覆盖”，任务创建默认 DRAFT。
- 2026-04-23：确认总体技术路线采用“任务域彻底重建，交易链路复用”的方案 B。
- 2026-04-23：补充 `BUY_COUNT` 模式的防超买约束：下单前必须先预占购买名额，失败或回收时回补名额。
- 2026-04-24：新增《外部仓库借鉴方案.md》，补充外部仓库在启停、防超买、实时进度与执行流水线上的可借鉴做法。
- 2026-04-24：确认仓库中已落地 C5 扫货 2.0 的 migration、后端接口、任务服务、调度器、执行器、命中/下单明细和前端页面。
- 2026-04-24：根据实际代码更新《扫货2.0重构设计.md》，将状态从“待进入实现计划”调整为“核心功能已部分落地”。
- 2026-04-24：补充当前实现口径：账号并发上限首版固定为 1；调度器每 1000ms 扫描 READY 任务；任务进入 RUNNING 后持续循环；连续异常达到 3 次进入 ERROR。
- 2026-04-24：补充当前 C5 协议口径：`listing_id` 在 C5 首版中实际对应 C5 `productId`；当前下单通过 `TradeOrderRecord.outTradeNo` 匹配 C5 成功/失败响应。
- 2026-04-24：补充当前实现缺陷与后续 P0/P1 优化项：账号归属校验、attempt 保存 `out_trade_no`、预占绑定 attempt、可取消停用、单轮占槽调度、账号在途约束和 attempt 级启动恢复。
- 2026-04-24：新增 `V2026.04.24.004__c5_sniping_v2_attempt_recovery_fields.sql`，补齐 attempt 的 `out_trade_no`、`slot_reserved`、`slot_released` 与恢复索引；SQL 仅写入 Flyway migration，未在本地执行。
- 2026-04-24：新增 `V2026.04.24.005__c5_sniping_v2_remaining_runtime_fields.sql`，补齐任务 `stop_requested`、`stop_requested_at`、`next_scan_at` 以及 attempt `in_flight_amount`、`remote_checked_at`；SQL 仅写入 Flyway migration，未在本地执行。
- 2026-04-24：后端补齐 C5 账号归属校验、attempt 级 `outTradeNo`、预占归属、预占释放防泄漏、可取消停用、单轮占槽调度、在途金额扣减和启动恢复远端订单保守核验。
- 2026-04-24：前端将任务筛选与表单账号输入改为 C5 账号选择器，并展示停止请求、下次扫描、attempt 在途金额和远端核验时间。
- 2026-04-24：后端主源码编译通过；完整 `clean install -DskipTests` 受既有测试源码 BOM 问题阻塞，阻塞文件为 `niro-server/niro-web/src/test/java/com/niro/web/controller/RbacManageControllerTest.java`。
- 2026-04-24：新增 C5 扫货 2.0 SSE 运行态推送接口 `GET /api/c5/sniping/v2/tasks/events`，后端在任务启停、调度、终态、命中和 attempt 状态变化后发布用户维度事件。
- 2026-04-24：前端 C5 扫货 2.0 页面接入 SSE 订阅，收到运行态事件后 debounce 刷新任务列表，并在当前明细抽屉匹配时刷新命中或下单尝试明细。
- 2026-04-24：修正 SSE 事件发送为默认 message 事件，保证前端原生 EventSource `onmessage` 可接收；后端主源码编译通过。
- 2026-04-24：新增 `V2026.04.24.006__c5_sniping_v2_scheduler_runtime_guard.sql`，补齐任务 lease 字段、账号运行态表、attempt `init_expire_at` / `recovery_attempt_count` 以及相关调度和过期 INIT 索引；SQL 仅写入 Flyway migration，未在本地执行。
- 2026-04-24：后端补齐账号并发上限配置化，调度器按账号运行态 `concurrencyLimit` 分配 READY 任务，默认并发为 1。
- 2026-04-24：后端补齐跨实例调度 lease，READY 任务启动前需要抢占数据库 lease，`markRunning` 校验 lease owner 和过期时间，启动结束后按 owner 清理 lease。
- 2026-04-24：后端补齐账号级在途下单数限制和账号短冷却，在账号 Redisson watchdog 锁内检查 INIT attempt 数，C5 空响应、远端失败或下单接口异常会写入短冷却。
- 2026-04-24：后端补齐长期 INIT attempt 处理策略，创建 attempt 时写入 `init_expire_at`，调度器周期核验过期 INIT，达到恢复阈值后以 `INIT_TIMEOUT` 收口，并周期补偿已终态但预占未结算的 attempt。
- 2026-04-24：根据最新实现更新《索引.md》和《扫货2.0重构设计.md》，将账号并发配置、账号在途限制、账号冷却、跨实例 lease 和长期 INIT 超时核验从待实现调整为已落地。
- 2026-04-26：根据新需求将 C5 扫货账号从共享 `buff_account` 中拆出，新增独立表 `c5_sniping_account`，扫货账号配置页和 C5 扫货 2.0 任务选账号链路统一切换到新账号模型，并移除扫描号 / 下单号 / 全能号角色语义。
- 2026-04-26：确认 C5 扫货 2.0 不再使用用户全局 C5 AppKey 与 Steam 交易链接，改为每个 `c5_sniping_account` 单独配置 `c5AppKey` 和 `steamTradeUrl`；账号配置页移除一键检测。
- 2026-04-26：新增《扫货配置完整链路.md》，确认本轮配置闭环要补齐账号运行态配置入口、任务页可用账号接口和账号绑定任务提示；旧 `TaskConfig` / `TaskList` 不再作为扫货配置入口。
- 2026-04-26：完成扫货配置完整链路实现，后端账号接口支持运行态配置与绑定任务提示，前端账号配置页只展示用户关心的账号凭据与绑定提示，不再暴露并发上限和最大在途下单数，任务页可用账号接口切换为 `/api/c5/sniping/v2/tasks/accounts`；后端编译通过，前端 type-check 仍受既有类型问题阻塞。
- 2026-04-26：任务配置中的请求频率改为秒单位输入，后端 `scanIntervalMs` 最小边界从 200ms 调整为 1 秒，接口与数据库字段仍保持毫秒口径。
