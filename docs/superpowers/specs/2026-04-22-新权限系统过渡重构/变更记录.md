# 新权限系统过渡重构变更记录

- 2026-04-22：建立“新权限系统过渡重构”主题目录，确认采用新旧系统并行的过渡方案。
- 2026-04-22：确认新权限系统与旧系统区分规则：新页面、组命名统一加 `new`，新接口统一走 `v2`。
- 2026-04-22：确认并行阶段数据边界：复用用户、角色；菜单、按钮、授权关系与发布态采用新结构。
- 2026-04-22：确认新路由模式为混合模式，资源模型为“路由/菜单绑定 + 按钮独立”。
- 2026-04-22：确认新权限管理页采用资源工作台结构，用户分配通过辅助抽屉承载。
- 2026-04-22：确认试点与切换策略：先迁移权限相关页面，再以 `InviteCodeManageNew`、`LogsNew` 作为业务试点，并通过侧栏新增 `new` 菜单组进入。
- 2026-04-22：确认一期数据模型采用极简双表发布模型，发布只保留单一全局草稿与单一已发布态，不引入发布批次、历史版本、回滚与审批流。
- 2026-04-22：确认页面注册表是新路由系统真源，后端仅保存 `pageKey`，不保存 `path`、`component`、`routeName` 等路由定义。
- 2026-04-22：确认 `v2` 接口采用细 REST 风格，最小闭环包含资源查询、资源编辑、角色授权、发布前校验、执行发布、新菜单组导航查询、当前登录用户已发布按钮权限查询。
- 2026-04-22：确认新系统发布后采用轻量版本比对自动失效，前端进入 `new` 链路时按 `config_version` 或 `published_at` 比对缓存版本。
- 2026-04-22：确认试点页保留独立新入口与独立新权限链路，但页面主体实现优先复用旧页已有逻辑。
- 2026-04-22：确认前端 Phase 2 采用最小侵入接法：旧路由与旧缓存保持不动，新增 `page registry`、`new permission store` 与 `new route builder` 承接 `new` 菜单组。
- 2026-04-22：确认新链路缓存与失败边界独立：版本变化时只清新缓存；新导航或新按钮接口失败时不清 token、不影响旧系统访问。
- 2026-04-22：确认新按钮权限接口口径改为“当前登录用户已发布按钮权限查询”，不再由前端直接以 `roleId` 作为消费口径。
- 2026-04-24：对齐当前代码进度：后端 `v2` 接口、四表模型、草稿/发布态覆盖发布、发布前校验、角色预览、已发布导航与按钮权限查询已落地。
- 2026-04-24：对齐当前代码进度：前端 `pageRegistry`、`new permission store`、`new route builder`、`new` 菜单组装配和新按钮权限消费链路已落地。
- 2026-04-24：对齐当前代码进度：`PermissionManageNew`、`InviteCodeManageNew`、`LogsNew` 已完成试点接入，补充 migration 已种入三者 PAGE 资源并给 admin 角色写入草稿态与发布态授权。
- 2026-04-24：记录剩余差异：试点页按钮资源未做初始化种子，需要通过新权限工作台配置后发布；`C5SnipingTaskV2` 已进入前端页面注册表，但尚未纳入本主题一期试点种子与授权范围。
- 2026-04-25：确认 `PermissionManageNew` 菜单权限 Tab 采用“页面 + 菜单树”口径，PAGE 作为模块根节点，MENU 作为树形授权项，BUTTON 资源继续由操作权限 Tab 承接；本次不新增接口、不改变保存发布机制。
- 2026-04-25：曾确认进入旧权限系统全量替换规划阶段，并短暂采用旧 `/api/user/menus`、`sys_menu`、`sys_role_menu` 作为第一阶段回退兜底；该口径随后被干净切换决策取代。
- 2026-04-25：确认 Phase 1 模型增强采用目录型 `MENU`，不新增 `GROUP` 类型；`MENU` 允许无 `pageKey` 作为导航目录，`BUTTON` 仍需能归属到有效页面链路。
- 2026-04-25：完成 Phase 1 代码落地：后端发布校验允许目录型 `MENU`，前端新路由构建支持无 `pageKey` 菜单作为 `ParentView` 目录，`PermissionManageNew` 支持创建和编辑目录型菜单。
- 2026-04-25：确认旧权限系统采用干净切换口径，不保留旧 `/api/user/menus` 运行时兜底，旧权限页面、旧 RBAC 菜单维护入口和旧菜单权限后端链路从系统中移除。
- 2026-04-25：确认旧权限表直接删除范围为 `sys_role_menu` 与 `sys_menu`；`sys_user`、`sys_role`、`sys_user_role` 继续作为用户、角色与用户角色关系真源保留。
- 2026-04-26：确认全量按钮权限迁移与超级管理员锁定方案：后台业务按钮进入新 `BUTTON` 资源；`role_key = 'admin'` 角色默认拥有全部资源，权限页全选禁用，后端拒绝保存其授权修改，发布时自动补齐全量授权；现有 `userId = 1` 管理兜底本期保留。
