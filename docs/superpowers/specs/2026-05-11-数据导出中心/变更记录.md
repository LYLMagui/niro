# 变更记录

## 2026-05-11
- 初始化“数据导出中心”主题目录。
- 明确首期范围：通用导出中心框架 + 开箱记录作为首个真实接入业务。
- 确认导出任务只在业务页发起；数据导出页面只查看历史、筛选、手动刷新和下载。
- 确认文件目标存储为 MinIO，本次仅沉淀方案，待 MinIO 搭建完成后再开始实现。
- 确认批量导出语义为“导出当前筛选结果”，不做勾选导出。
- 确认任务状态机首期仅保留 `PENDING / PROCESSING / SUCCESS / FAILED`。
- 确认导出任务表核心字段包含 `user_id`、`request_params`、`file_object_key`、`file_url`、`fail_reason`、`created_at`、`updated_at`。
- 确认 `file_url` 为平台稳定下载地址，不直接保存 MinIO 临时签名链接。
- 确认“数据导出”页面挂在“统计”菜单下，并新增页面查看权限与开箱记录导出创建权限。
