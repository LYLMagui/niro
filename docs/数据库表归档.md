## 系统用户表
```sql
create table sys_user (
    id bigint generated always as identity primary key,
    username varchar(50) not null unique,
    password varchar(100) not null default '',
    nickname varchar(50) not null default '',
    email varchar(100) not null default '',
    avatar varchar(100) not null default '',
    status smallint default 1,
    is_delete smallint not null default 0,
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table sys_user is '系统用户表';
comment on column sys_user.id is '主键';
comment on column sys_user.username is '用户名';
comment on column sys_user.password is '密码';
comment on column sys_user.nickname is '昵称';
comment on column sys_user.email is '邮箱';
comment on column sys_user.avatar is '头像';
comment on column sys_user.status is '状态: 1-正常, 0-禁用';
comment on column sys_user.is_delete is '删除: 0-否, 1-是';
comment on column sys_user.create_time is '创建时间';
comment on column sys_user.update_time is '更新时间';
```

## csgo饰品分类表
```sql
create table buff_goods_category (
    id bigint generated always as identity primary key,
    name varchar(100) not null default '',
    parent_id bigint not null default 0,
    internal_name varchar(100) not null default '',
    full_internal_name varchar(100) not null default '',
    category_type varchar(50) not null default 'type',
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table buff_goods_category is 'csgo饰品分类表';
comment on column buff_goods_category.id is '主键';
comment on column buff_goods_category.name is 'api传入标识';
comment on column buff_goods_category.parent_id is '父分类ID';
comment on column buff_goods_category.internal_name is '内部标识';
comment on column buff_goods_category.full_internal_name is 'api传入标识-全称';
comment on column buff_goods_category.category_type is '参数类型: type(对应category参数), weapon(对应weapon参数)';
comment on column buff_goods_category.create_time is '创建时间';
comment on column buff_goods_category.update_time is '更新时间';

create index idx_category_parent on buff_goods_category(parent_id);
create index idx_category_name on buff_goods_category(name);
create unique index idx_category_internal_name on buff_goods_category(internal_name);
create index idx_category_full_internal_name on buff_goods_category(full_internal_name);
create index idx_category_type on buff_goods_category(category_type);

-- 初始化基础数据
insert into buff_goods_category (id, name, parent_id) overiding system value values (2969, '匕首', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2970, '手套', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2971, '步枪', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2972, '手枪', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2973, '微型冲锋枪', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2974, '霰弹枪', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2975, '机枪', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2976, '印花', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2977, '挂件', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2978, '探员', 0);
insert into buff_goods_category (id, name, parent_id) overiding system value values (2979, '其他', 0);
```

## csgo商品表
```sql
create table buff_goods (
    id bigint generated always as identity primary key,
    goods_id bigint not null unique,
    name varchar(255) not null default '',
    short_name varchar(255) not null default '',
    internal_name varchar(255) not null default '',
    category_id bigint not null default 0,
    rarity varchar(100) not null default '',
    exterior varchar(100) not null default '',
    market_hash_name varchar(255) not null default '',
    icon_url text not null default '',
    original_icon_url text not null default '',
    tags jsonb not null default '{}'::jsonb,
    last_sync_tag varchar(50) not null default '',
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table buff_goods is 'csgo商品表';
comment on column buff_goods.id is '主键';
comment on column buff_goods.goods_id is '对应 buff 的 goods_id';
comment on column buff_goods.name is '商品全名';
comment on column buff_goods.short_name is '简称';
comment on column buff_goods.internal_name is '内部标识';
comment on column buff_goods.category_id is '分类ID';
comment on column buff_goods.rarity is '稀有度';
comment on column buff_goods.exterior is '外观/大磨损分类';
comment on column buff_goods.market_hash_name is 'steam市场hash名称';
comment on column buff_goods.icon_url is '图标URL';
comment on column buff_goods.original_icon_url is '原始图标URL';
comment on column buff_goods.tags is '标签信息(jsonb)';
comment on column buff_goods.last_sync_tag is '最后同步版本标识';
comment on column buff_goods.create_time is '创建时间';
comment on column buff_goods.update_time is '更新时间';

create index idx_goods_category_id on buff_goods(category_id);
create index idx_goods_market_hash_name on buff_goods(market_hash_name);
create index idx_goods_last_sync_tag on buff_goods(last_sync_tag);
create index idx_goods_update_time on buff_goods(update_time);
create index idx_goods_create_time on buff_goods(create_time);
```

## 商品价格历史表
```sql
create table buff_price_history (
    id bigint generated always as identity primary key,
    goods_id bigint not null,
    price decimal(10, 2) not null default 0.00,
    buy_max_price decimal(10, 2) not null default 0.00,
    sell_num integer not null default 0,
    record_time timestamptz not null default current_timestamp,
    create_time timestamptz not null default current_timestamp
);

comment on table buff_price_history is '商品价格历史表';
comment on column buff_price_history.id is '主键';
comment on column buff_price_history.goods_id is '关联商品表';
comment on column buff_price_history.price is '当前最低出售价';
comment on column buff_price_history.buy_max_price is '当前最高求购价';
comment on column buff_price_history.sell_num is '在售数量';
comment on column buff_price_history.record_time is '记录时间';
comment on column buff_price_history.create_time is '创建时间';

create index idx_price_history_goods_id on buff_price_history(goods_id);
create index idx_price_history_record_time on buff_price_history(record_time);
create unique index idx_price_history_dedup on buff_price_history(goods_id, record_time);
```

## 扫货任务配置表
```sql
create table buff_scan_task (
    id bigint generated always as identity primary key,
    name varchar(255) not null default '',
    user_id bigint not null default 0,
    goods_id bigint,
    max_price decimal(10, 2),
    min_paintwear decimal(10, 6) not null default 0.000000,
    max_paintwear decimal(10, 6) not null default 1.000000,
    buy_count integer not null default 1,
    status integer not null default 0,
    cron_expression varchar(100),
    duration_minutes integer not null default 0,
    scan_interval integer not null default 5,
    scan_interval_min integer,
    scan_interval_max integer,
    rest_period integer not null default 0,
    task_type integer not null default 0,
    min_profit decimal(10, 2) not null default 0.00,
    run_mode varchar(20) not null default 'BOTH',
    target_trade_account_id bigint,
    target_task_id bigint not null default 0,
    platform varchar(32) not null default 'BUFF',
    extra_config text,
    safety_margin numeric(10, 4),
    ladder_step numeric(10, 4),
    last_error text,
    finish_time timestamptz,
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table buff_scan_task is '扫货任务配置表';
comment on column buff_scan_task.id is '主键';
comment on column buff_scan_task.name is '任务名称(通常是商品名)';
comment on column buff_scan_task.user_id is '创建用户ID';
comment on column buff_scan_task.goods_id is 'buff商品id (关联 buff_goods.goods_id)';
comment on column buff_scan_task.max_price is '目标最高价格(包含)';
comment on column buff_scan_task.min_paintwear is '最小磨损(包含)';
comment on column buff_scan_task.max_paintwear is '最大磨损(包含)';
comment on column buff_scan_task.buy_count is '计划购买数量';
comment on column buff_scan_task.status is '任务状态: 0-停止, 1-运行中, 2-已完成, 3-异常, 4-系统任务, 5-定时等待中';
comment on column buff_scan_task.cron_expression is 'cron触发表达式(空则立即开始)';
comment on column buff_scan_task.duration_minutes is '单次运行时长(分钟)/工作周期';
comment on column buff_scan_task.scan_interval is '运行期间的扫描间隔(秒)';
comment on column buff_scan_task.scan_interval_min is '最小扫描间隔(秒)';
comment on column buff_scan_task.scan_interval_max is '最大扫描间隔(秒)';
comment on column buff_scan_task.rest_period is '暂停时长(分钟)';
comment on column buff_scan_task.task_type is '任务类型: 0-炼金扫货, 1-站内倒卖, 2-分类同步, 3-商品全量同步';
comment on column buff_scan_task.min_profit is '站内倒卖任务的最小预期利润';
comment on column buff_scan_task.run_mode is '运行模式: scan-仅扫描, trade-仅交易, both-全能模式';
comment on column buff_scan_task.target_trade_account_id is '关联的下单账号id (仅 scan 模式任务使用)';
comment on column buff_scan_task.target_task_id is '关联的下单任务id (仅 scan/both 模式使用)';
comment on column buff_scan_task.platform is '所属平台: buff/c5';
comment on column buff_scan_task.extra_config is '平台特殊配置(json)';
comment on column buff_scan_task.safety_margin is '安全边际 (用于价格锚定)';
comment on column buff_scan_task.ladder_step is '锚定阶梯 (价格调整步长)';
comment on column buff_scan_task.last_error is '最近一次任务运行失败的错误简述';
comment on column buff_scan_task.finish_time is '任务完成时间';
comment on column buff_scan_task.create_time is '创建时间';
comment on column buff_scan_task.update_time is '更新时间';

create index idx_scan_task_status on buff_scan_task (status);
create index idx_scan_task_user on buff_scan_task (user_id);
create index idx_scan_task_run_mode on buff_scan_task (run_mode);
create index idx_scan_task_target_task_id on buff_scan_task (target_task_id);

-- 初始化系统同步任务
insert into buff_scan_task (name, user_id, task_type, status, cron_expression, scan_interval)
values ('系统-全量分类同步', 1, 2, 0, '0 0 3 * * ?', 0);
insert into buff_scan_task (name, user_id, task_type, status, cron_expression, scan_interval)
values ('系统-全量商品同步', 1, 3, 0, '0 0 4 * * ?', 0);
```

## 用户多平台配置表
```sql
create table user_platform_setting (
    id bigint generated always as identity primary key,
    user_id bigint not null default 0,
    payment_method varchar(50) not null default 'BALANCE',
    wecom_corpid varchar(100) not null default '',
    wecom_corpsecret varchar(255) not null default '',
    wecom_agentid varchar(100) not null default '',
    wecom_touser varchar(100) not null default '',
    email_enabled boolean not null default false,
    email_host varchar(100) not null default '',
    email_port integer not null default 465,
    email_account varchar(100) not null default '',
    email_password varchar(100) not null default '',
    email_receiver varchar(100) not null default '',
    c5_app_key varchar(255) not null default '',
    c5_secret_key varchar(255) not null default '',
    c5_trade_url varchar(500) not null default '',
    steam_trade_url varchar(500) not null default '',
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table user_platform_setting is '用户多平台配置表';
comment on column user_platform_setting.user_id is '用户ID';
comment on column user_platform_setting.payment_method is '支付方式: BALANCE-余额, ALIPAY-支付宝, WECHAT-微信';
comment on column user_platform_setting.wecom_corpid is '企业微信企业ID';
comment on column user_platform_setting.wecom_corpsecret is '企业微信应用Secret';
comment on column user_platform_setting.wecom_agentid is '企业微信应用AgentID';
comment on column user_platform_setting.wecom_touser is '企业微信接收人';
comment on column user_platform_setting.email_enabled is '是否开启邮件通知';
comment on column user_platform_setting.email_host is 'smtp服务器地址';
comment on column user_platform_setting.email_port is 'smtp端口';
comment on column user_platform_setting.email_account is '发件人邮箱账号';
comment on column user_platform_setting.email_password is '发件人邮箱授权码/密码';
comment on column user_platform_setting.email_receiver is '收件人邮箱';
comment on column user_platform_setting.c5_app_key is 'c5平台 api key';
comment on column user_platform_setting.c5_secret_key is 'c5平台 secret key';
comment on column user_platform_setting.c5_trade_url is 'c5平台交易链接';
comment on column user_platform_setting.steam_trade_url is 'steam交易链接';
```

## buff印花元数据及价值表
```sql
create table buff_sticker (
    id bigint generated always as identity primary key,
    sticker_id bigint not null default 0,
    name varchar(255) not null default '',
    image_url text not null default '',
    price decimal(10, 2) not null default 0.00,
    sell_num integer not null default 0,
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table buff_sticker is 'buff印花元数据及价值表';
comment on column buff_sticker.id is '主键ID';
comment on column buff_sticker.sticker_id is 'buff平台印花唯一标识id';
comment on column buff_sticker.name is '印花中文名称';
comment on column buff_sticker.image_url is '印花图片预览链接';
comment on column buff_sticker.price is '印花本体市场底价(用于计算溢价)';
comment on column buff_sticker.sell_num is '在售数量';
comment on column buff_sticker.create_time is '创建时间';
comment on column buff_sticker.update_time is '最后更新时间';

create unique index uk_sticker_id on buff_sticker(sticker_id);
create index idx_sticker_name on buff_sticker(name);
```

## 饰品市场行情统计雷达表
```sql
create table buff_goods_stats (
    id bigint generated always as identity primary key,
    goods_id bigint not null default 0,
    avg_price_7d decimal(10, 2) not null default 0.00,
    avg_price_24h decimal(10, 2) not null default 0.00,
    buy_max_price decimal(10, 2) not null default 0.00,
    sell_num integer not null default 0,
    liquidity_score integer not null default 0,
    update_time timestamptz not null default current_timestamp
);

comment on table buff_goods_stats is '饰品市场行情统计雷达表';
comment on column buff_goods_stats.id is '主键ID';
comment on column buff_goods_stats.goods_id is 'buff商品唯一标识id';
comment on column buff_goods_stats.avg_price_7d is '过去7天成交均价';
comment on column buff_goods_stats.avg_price_24h is '过去24小时成交均价';
comment on column buff_goods_stats.buy_max_price is '当前最高求购价格';
comment on column buff_goods_stats.sell_num is '当前在售数量';
comment on column buff_goods_stats.liquidity_score is '流动性评分(0-100，分数越高变现越快)';
comment on column buff_goods_stats.update_time is '最后统计时间';

create unique index uk_stats_goods_id on buff_goods_stats(goods_id);
create index idx_stats_liquidity on buff_goods_stats(liquidity_score);
```

## 捡漏预警触发日志表
```sql
create table buff_leak_alert (
    id bigint generated always as identity primary key,
    task_id bigint not null default 0,
    goods_id bigint not null default 0,
    sell_id varchar(100) not null default '',
    price decimal(10, 2) not null default 0.00,
    expected_profit decimal(10, 2) not null default 0.00,
    reason text not null default '',
    is_bought smallint not null default 0,
    create_time timestamptz not null default current_timestamp
);

comment on table buff_leak_alert is '捡漏预警触发日志表';
comment on column buff_leak_alert.id is '主键ID';
comment on column buff_leak_alert.task_id is '触发该预警的扫描任务ID';
comment on column buff_leak_alert.goods_id is '商品ID';
comment on column buff_leak_alert.sell_id is 'buff平台该笔上架单的唯一标识';
comment on column buff_leak_alert.price is '触发预警时的挂牌价格';
comment on column buff_leak_alert.expected_profit is '预估理论利润额';
comment on column buff_leak_alert.reason is '触发逻辑简述(如：低磨/印花溢价/超低价)';
comment on column buff_leak_alert.is_bought is '是否已购买(0:未购, 1:已购)';
comment on column buff_leak_alert.create_time is '触发预警的时间';

create index idx_alert_goods_id on buff_leak_alert(goods_id);
create index idx_alert_task_id on buff_leak_alert(task_id);
create index idx_alert_create_time on buff_leak_alert(create_time);
```

## buff 账号配置表
```sql
create table buff_account (
    id bigint generated always as identity primary key,
    user_id bigint not null default 0,
    account_name varchar(64) not null default '',
    buff_cookie text not null default '',
    role varchar(20) not null default 'SCAN',
    status varchar(20) not null default 'NORMAL',
    weight integer not null default 1,
    balance numeric(10, 2) not null default 0.00,
    pending_balance numeric(10, 2) not null default 0.00,
    fail_count integer not null default 0,
    today_scan_count integer not null default 0,
    trade_success_count integer not null default 0,
    trade_total_count integer not null default 0,
    last_check_time timestamptz not null default current_timestamp,
    user_agent varchar(512) not null default '',
    remark varchar(255) not null default '',
    warning_msg varchar(512) not null default '',
    is_deleted smallint not null default 0,
    platform varchar(32) not null default 'BUFF',
    api_config text,
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table buff_account is 'buff 账号配置表';
comment on column buff_account.id is '主键id';
comment on column buff_account.user_id is '系统用户id';
comment on column buff_account.account_name is '账号备注名';
comment on column buff_account.buff_cookie is 'buff 登录凭证';
comment on column buff_account.role is '账号角色：scan-扫描, trade-下单, both-全能';
comment on column buff_account.status is '账号状态：normal-正常, banned-封禁, market_restricted-市场访问限制, trade_restricted-下单限制, invalid-失效';
comment on column buff_account.weight is '调度权重';
comment on column buff_account.balance is '账号余额';
comment on column buff_account.pending_balance is '待结算余额';
comment on column buff_account.fail_count is '连续请求失败计数';
comment on column buff_account.today_scan_count is '今日扫货总数';
comment on column buff_account.trade_success_count is '下单成功总数';
comment on column buff_account.trade_total_count is '下单总数(用于计算成功率)';
comment on column buff_account.last_check_time is '最后有效性检测时间';
comment on column buff_account.user_agent is '关联的浏览器指纹';
comment on column buff_account.remark is '用户自定义备注信息';
comment on column buff_account.warning_msg is '异常说明';
comment on column buff_account.is_deleted is '逻辑删除标记';
comment on column buff_account.platform is '所属平台: buff/c5';
comment on column buff_account.api_config is 'api配置信息(json)';
comment on column buff_account.create_time is '创建时间';
comment on column buff_account.update_time is '更新时间';

create index idx_account_user_id on buff_account (user_id);
create index idx_account_status on buff_account (status);
create index idx_account_role on buff_account (role);
create unique index uk_idx_single_sniper_account on buff_account (user_id) where (role in ('TRADE', 'BOTH') and is_deleted = 0);
```

## 任务与账号关联表
```sql
create table buff_scan_task_account (
    id bigint generated always as identity primary key,
    task_id bigint not null,
    account_id bigint not null,
    create_time timestamptz not null default current_timestamp,
    constraint uk_task_account unique (task_id, account_id)
);

comment on table buff_scan_task_account is '任务与账号关联表';
comment on column buff_scan_task_account.id is '主键ID';
comment on column buff_scan_task_account.task_id is '任务ID';
comment on column buff_scan_task_account.account_id is '账号ID';
comment on column buff_scan_task_account.create_time is '创建时间';

create index idx_task_account_task_id on buff_scan_task_account(task_id);
create index idx_task_account_account_id on buff_scan_task_account(account_id);
```

## 多平台交易订单记录表
```sql
create table trade_order_record (
    id bigint generated always as identity primary key,
    user_id bigint not null default 0,
    task_id bigint not null default 0,
    account_id bigint not null default 0,
    platform varchar(32) not null default 'BUFF',
    goods_id bigint,
    goods_name varchar(255) not null default '',
    market_hash_name varchar(255) not null default '',
    goods_img varchar(512) not null default '',
    order_id varchar(64) not null default '',
    out_trade_no varchar(64) not null default '',
    price decimal(10, 2) not null default 0.00,
    paintwear decimal(10, 8) not null default 0.0000,
    status smallint not null default 0,
    error_msg varchar(512) not null default '',
    error_code varchar(64) not null default '',
    extra_info jsonb not null default '{}'::jsonb,
    create_time timestamptz not null default current_timestamp,
    update_time timestamptz not null default current_timestamp
);

comment on table trade_order_record is '多平台交易订单记录表';
comment on column trade_order_record.user_id is '用户ID';
comment on column trade_order_record.task_id is '关联的任务ID';
comment on column trade_order_record.account_id is '使用的账号ID';
comment on column trade_order_record.platform is '平台标识: buff, c5';
comment on column trade_order_record.goods_id is '商品ID';
comment on column trade_order_record.goods_name is '商品显示名称';
comment on column trade_order_record.market_hash_name is '唯一hash名';
comment on column trade_order_record.goods_img is '商品图片';
comment on column trade_order_record.order_id is '平台侧订单号';
comment on column trade_order_record.out_trade_no is '系统内部请求流水号';
comment on column trade_order_record.price is '下单价格';
comment on column trade_order_record.paintwear is '磨损值(无磨损则为0)';
comment on column trade_order_record.status is '状态: 0-处理中, 1-成功, 2-失败, 3-取消';
comment on column trade_order_record.error_msg is '失败原因';
comment on column trade_order_record.error_code is '平台返回的错误码';
comment on column trade_order_record.extra_info is '扩展信息(jsonb)';
comment on column trade_order_record.create_time is '创建时间';
comment on column trade_order_record.update_time is '更新时间';

create index idx_trade_order_user on trade_order_record (user_id);
create index idx_trade_order_task on trade_order_record (task_id);
create index idx_trade_order_time on trade_order_record (create_time);
create index idx_trade_order_platform on trade_order_record (platform);
create index idx_trade_order_out_trade_no on trade_order_record (out_trade_no);
```

## 角色信息表
```sql
create table sys_role (
    role_id bigint generated always as identity primary key,
    role_name varchar(30) not null,
    role_key varchar(100) not null,
    role_sort integer default 0,
    data_scope char(1) default '1',
    status smallint default 1,
    del_flag smallint default 0,
    create_by varchar(64) default '',
    create_time timestamptz,
    update_by varchar(64) default '',
    update_time timestamptz,
    remark varchar(500) default null
);

comment on table sys_role is '角色信息表';
comment on column sys_role.role_id is '角色ID';
comment on column sys_role.role_name is '角色名称';
comment on column sys_role.role_key is '角色权限字符串';
comment on column sys_role.status is '角色状态(1正常 0停用)';

create index idx_sys_role_del_flag on sys_role (del_flag);
```

## 菜单权限表
```sql
create table sys_menu (
    menu_id bigint generated always as identity primary key,
    menu_name varchar(50) not null,
    parent_id bigint default 0,
    order_num integer default 0,
    path varchar(200) default '',
    component varchar(255) default null,
    query varchar(255) default null,
    is_frame smallint default 0,
    is_cache smallint default 0,
    menu_type char(1) default '',
    visible smallint default 1,
    status smallint default 1,
    perms varchar(100) default null,
    icon varchar(100) default '#',
    create_by varchar(64) default '',
    create_time timestamptz,
    update_by varchar(64) default '',
    update_time timestamptz,
    remark varchar(500) default ''
);

comment on table sys_menu is '菜单权限表';
comment on column sys_menu.menu_name is '菜单名称';
comment on column sys_menu.path is '路由地址';
comment on column sys_menu.component is '组件路径';
comment on column sys_menu.menu_type is '菜单类型(m目录 c菜单 f按钮)';
comment on column sys_menu.perms is '权限标识';

create index idx_sys_menu_parent_id on sys_menu (parent_id);
create index idx_sys_menu_status on sys_menu (status);
```

## 用户和角色关联表
```sql
create table sys_user_role (
    user_id bigint not null,
    role_id bigint not null,
    primary key (user_id, role_id)
);

comment on table sys_user_role is '用户和角色关联表';
```

## 角色和菜单关联表
```sql
create table sys_role_menu (
    role_id bigint not null,
    menu_id bigint not null,
    primary key (role_id, menu_id)
);

comment on table sys_role_menu is '角色和菜单关联表';
```
