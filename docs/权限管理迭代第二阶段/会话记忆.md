# 权限管理迭代第二阶段会话记忆

最后更新：2026-03-22

## 当前关注

- M1/M2/M3/M4 已完成，当前主线聚焦 M5（测试回归收口）与 R-01/R-02 联合回归。
- 非 Buff 权限码迁移已完成：代码常量与数据库迁移脚本已对齐，待在更多环境执行回归验证。
- 前端权限管理页已形成可维护结构，后续重点是缺陷回归而非功能扩展。

## 已完成

- 已创建 `docs/权限管理迭代第二阶段/迭代计划.md`。
- 已创建 `docs/权限管理迭代第二阶段/会话记忆.md`。
- 已将第一阶段计划与记忆迁移到历史归档目录。
- 已将前端技能使用规则写入第二阶段计划与记忆。
- 已完成 `sys_menu.status` 全链路接入：
  - 后端实体 `SysMenu` 新增 `status`
  - RBAC 菜单 DTO 与接口返回新增 `status`
  - 菜单路由/权限聚合查询仅取 `status=1`
  - 角色菜单授权校验拦截停用菜单
- 已新增 PostgreSQL 脚本：`docker/postgres/migrations/rbac/2026-03-21-rbac-p2-menu-status-postgresql.sql`
- 已新增批量追加分配接口：`POST /api/rbac/users/roles/batch-append`
- 已实现角色菜单授权自动补祖先菜单（保存前补齐父级链路）
- 已完成角色强保护（K-03）：
  - 内置角色 `admin` 不可删除
  - 内置角色 `admin` 不可修改 `roleKey`
  - 内置角色 `admin` 不可停用
- 已完成删除兜底（K-04）：
  - 角色删除前校验用户绑定与菜单绑定
  - 菜单删除前校验子节点与角色绑定
- 已完成权限码规则（K-05）：
  - 非 Buff 权限码强制三段式小写冒号格式
  - 菜单权限码唯一性校验
- 已完成角色/菜单 CRUD（K-06/K-07）：
  - `GET/POST/PUT/DELETE /api/rbac/roles`
  - `GET/POST/PUT/DELETE /api/rbac/menus`
- 已完成 DTO/参数扩展（T-01/T-02/T-03）：
  - `RbacRoleDTO` 增加 `roleSort/remark`
  - `RbacMenuDTO` 增加 `name/path/component/icon/sortOrder/hidden/keepAlive/redirect`
  - 新增 `CreateRoleParam/UpdateRoleParam/CreateMenuParam/UpdateMenuParam`
- 已同步前端 RBAC API 契约：`niro-client/src/api/rbac.ts` 新增角色/菜单 CRUD 请求与类型。
- 已完成校验：
  - 后端：`mvn -pl niro-web -DskipTests clean compile`
  - 前端：`pnpm type-check`
- 已推进 M3 第一批前端落地：
  - 新增独立权限管理页 `niro-client/src/views/PermissionManage.vue`
  - `Settings` 页面移除嵌入式 RBAC 面板入口
  - 组件映射新增 `permission/rbac/rbacmanage` 到独立权限页
  - `RbacPermissionPanel` 重构为四分区（角色管理、菜单/按钮管理、角色授权、用户批量追加分配）
  - 权限变更统一提示“权限已更新，请相关用户刷新页面”
- 已完成本轮前端静态校验：`pnpm type-check`
- 已完成权限管理菜单数据库落地：
  - 新增脚本 `docker/postgres/migrations/rbac/2026-03-21-rbac-p2-add-permission-menu-postgresql.sql`
  - 已入库菜单 `权限管理`（`permission=system:permission:manage`）
  - 已补 `admin` 角色菜单授权
- 已完成非 Buff 权限码三段式迁移收口：
  - 新增脚本 `docker/postgres/migrations/rbac/2026-03-22-rbac-p2-permission-code-migration-non-buff-postgresql.sql`
  - 后端常量 `PermissionConstants` 已切换为 `system:settings:*` / `system:notify:send`
  - 前端常量 `PermissionConstant` 已切换为 `system:settings:*` / `system:notify:send`
- 已新增后端约束单测 `RbacManageControllerTest` 并通过 8 条用例：
  - 覆盖 admin 强保护、停用角色授权拦截、权限码格式/唯一性、删除绑定拦截、按钮父级校验
- 已修复独立权限页组件兼容报错（移除 `t-result` 依赖）并通过类型校验。

## 下一步

- 推进 M5：补齐后端测试 B-01/B-02/B-04/B-08 与前端交互测试 F-01~F-05 的“已完成”收口。
- 执行 R-01/R-02 联合回归，确认非 Buff 不回退、Buff 冻结不受影响。
- 完成“测试报告 + 回归结论”文档化，作为第二阶段关闭依据。

## 备注

- 本文件仅在你明确要求“记录/更新记忆”时维护。
- Git 提交信息必须使用中文。
