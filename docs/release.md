# Release Notes

## 2026-02-11 (v0.2.0)
- **RocketMQ 部署架构优化与监控集成**:
  - **多环境部署解耦**: 实现了 RocketMQ、FRP、XXL-JOB 等组件的配置解耦。将全局 `.env` 拆分至各组件目录，通过 `env_file` 相对路径引用，支持组件级的独立迁移与部署。
  - **远程连接优化**: 引入 `ROCKETMQ_IP` 环境变量动态注入 Broker 启动参数 (`-p brokerIP1`)，解决了远程服务器部署时的内网穿透与跨网段连接问题。
  - **监控面板集成**: 在 RocketMQ 编排中集成官方 `RocketMQ Dashboard` (端口 18080)，支持 Topic 管理、消费者监控及消息轨迹查询。
  - **运维安全增强**:
    - **磁盘空间保护**: 为所有 Docker 容器配置了日志轮转策略（100M * 3），防止日志文件无限制增长撑爆磁盘。
    - **服务高可用自愈**: 在 `broker.conf` 中配置了 `diskMaxUsedSpaceRatio=88` 磁盘警戒线，当宿主机硬盘空间不足时主动拒绝写入，防止系统级崩溃。
    - **配置标准化**: 为 `broker.conf` 补充了详细的中文注释，规范了数据保留时间、角色定义等核心参数。

## 2026-02-10 (v0.1.1)

- **C5 平台 API 冗余清理与稳定性修复**:
  - **冗余配置移除**: 彻底清理了项目中所有未使用的 `secretKey` 相关逻辑。包括 SDK 配置类 `C5Config`、业务服务 `C5TradeStrategyImpl`、持久化实体 `UserPlatformSettings` 以及配套的 DTO/Param 传输对象。
  - **接口请求方法修正**: 修复了 C5 批量查询买家订单状态接口 (`/merchant/order/v2/buyer/status`) 的请求方法，由 `GET` 修正为官方规范要求的 `POST`。
  - **订单详情状态映射优化**: 修复了前端 C5 订单详情界面中状态显示不准确的问题。新增了对 `status=200`（成功）和 `status=11`（失败）状态码的正确解析与颜色主题适配，消除了“未知(200)”的显示异常。
  - **工程质量**: 完成了全量代码编译验证，确保重构后的系统在移除冗余配置后保持 100% 的构建稳定性。

## 2026-02-09 (v0.1.0)

- **C5 开放平台接口迁移与快慢路由策略实现**:
  - **接口 V2 迁移**: 将 C5 旧版“根据 MarketHashName 查询在售列表”接口迁移至官方推荐的 V2 版本。SDK 层新增 `C5ProductListRequest`、`C5ProductListResponse` 及其配套 DTO。
  - **快慢接口路由策略**:
    - **快接口 (`/list`)**: 针对非磨损类商品（Other 分类）实现高 QPS (5/s) 快速轮询。
    - **慢接口 (`/search`)**: 针对磨损类商品（枪械、手套）实现低 QPS (1/s) 精确筛选，支持 `wearMin`、`wearMax`、`priceMax` 等多维度参数过滤。
  - **业务层逻辑重构**:
    - 重构了 `C5TradeStrategyImpl` 核心逻辑，移除了原有的并行查询（人工/自动发货分流）机制，改为利用新接口的全量返回能力实现单次请求覆盖，显著降低 QPS 压力。
    - 优化了磨损数据读取逻辑，优先采用 `assetInfo.getFloatWear()` 提升精度。
    - 增强了异常处理机制，引入 `updateLastError` 实时记录任务执行异常。
  - **SDK 质量保障**: 完善了 `C5MarketClient` 单元测试，引入 Mockito 模拟 HTTP 引擎，确保了 100% 的接口映射准确性与响应解析稳定性。

## 2026-02-05 (v2.29.3)

- **动态路由重构完善与全栈类型修复**:
  - **后端实体一致性修复**: 修复了 `SysMenu` 实体类中 `create_by` 等审计字段不存在导致的数据库查询异常；将 `hidden` 和 `keepAlive` 字段类型由 `Integer` 修正为 `Boolean`，确保与 PostgreSQL 布尔类型完美兼容。
  - **前端类型安全增强**: 补全了 `RouterMeta` 接口定义（增加 `alwaysShow`、`link`、`breadcrumb` 等字段），修复了前端路由转换逻辑中的 10+ 处 TypeScript 类型报错。
  - **UI 图标标准化**: 统一了“全链路日志”模块的图标显示，前端组件与菜单配置同步切换至 `BulletpointIcon` (list)，解决了图标不一致带来的视觉偏差。
  - **构建质量验证**: 完成了全量代码编译验证（`mvn compile` 与 `pnpm type-check`），确保重构后的系统具备生产级稳定性。

## 2026-02-04 (v2.29.2)

- **动态路由与布局系统重构**:
  - **消除双重嵌套**: 彻底修复了由于静态路由与动态路由配置重叠导致的界面 Layout 双重嵌套现象，遵循“顶级节点控制布局”原则。
  - **后端组件判定优化**: 重构了 `SysMenuServiceImpl.buildComponent` 逻辑，确保顶级菜单始终返回 `Layout`，目录节点返回 `ParentView`。
  - **前端路由精简**: 移除了静态路由中的冗余 Layout 配置，优化了 `componentMap.ts` 的组件映射与回退机制，严禁在叶子节点返回 Layout。
  - **权限存储增强**: 优化了 `permission.ts` 中的路由过滤算法，支持一级菜单自动推导业务组件，确保布局的一致性。
  - **全栈代码质量**: 修复了重构引入的 TypeScript 类型错误与未使用变量警告，通过了全量编译验证。

## 2026-02-03 (v2.29.1)

- **后端数据库查询重构 (MyBatis-Plus)**:
  - **链式调用标准化**: 对 `SysMenuService`, `SysRoleService`, `TradeOrderRecordService`, `C5TradeStrategy`, `C5TaskStartupRunner` 等核心模块进行了深度重构，将所有手动实例化的 `Wrapper` 替换为 `lambdaQuery()/lambdaUpdate()` 链式调用，提升代码可读性。
  - **依赖注入规范化**: 修复了 Service 层跨层直接调用 Mapper 的反模式，通过引入 `SysUserRoleService` 和 `SysRoleMenuService` 实现层级解耦，严格遵循构造注入原则。
  - **业务逻辑增强**: 为 `TradeOrderRecordService` 新增 `countSuccess` 接口并实现复杂条件查询的链式封装，支持 C5 策略模块的高效调用。

## 2026-02-01 (v2.29.0)

- **交易记录管理功能增强**:
  - **多维排序与过滤**: 实现了交易记录列表的按价格、时间、状态的多维排序，以及按平台、状态的筛选功能，提升数据对账效率。
  - **C5 订单详情集成**: 新增 C5 平台订单详情穿透查询，支持查看实际支付金额、详细错误描述及扩展 JSON 字段。
  - **记录维护与清理**: 实现了交易记录的单条逻辑删除功能，配备二次确认机制，支持历史数据维护。
- **全栈类型安全与代码质量优化**:
  - **前端类型补全**: 修复了 `OrderRecord.vue`, `Logs.vue`, `TaskConfig.vue` 等核心组件中 40+ 处 TypeScript 类型错误，补全了 `TradeOrderRecord`, `LogItem` 等核心实体的字段定义。
  - **组件库适配**: 解决了 TDesign 组件库在 `:theme` 赋值、`v-model` 类型转换等方面的 TS 兼容性问题，确保前端构建 (`pnpm type-check`) 100% 通过。
  - **配置架构对齐**: 统一了 `UserPlatformSettings` 在前后端的定义，修复了由于接口模型不一致导致的配置页面白屏风险。
  - **冗余代码清理**: 移除了 `OrderRecord.vue` 中未使用的编辑表单、商品查询及 Watcher 逻辑，遵循“最小修改”与“职责单一”原则。

## 2026-02-01 (v2.28.0)

- **RBAC 权限系统与动态路由重构 (Phase 1)**:
  - **核心模型实现**: 引入了标准的 RBAC 五表模型（`SysMenu`, `SysRole`, `SysUserRole`, `SysRoleMenu`），支持基于角色的精细化权限控制。
  - **动态路由后端支持**: 新增 `SysMenuController` 及其配套服务，实现了 `GET /api/user/menus` 接口，支持将数据库定义的菜单树转换为前端 Vue Router 格式。
  - **工程规范增强**:
    - **实体类优化**: 移除了 `BuffScanTask` 等实体类中的冗余字段，保持与最新数据库 Schema 同步。
    - **日志系统升级**: 数据库层切换至 `Slf4jImpl` 日志实现，支持通过日志级别精确控制 SQL 输出，解决控制台日志冗余问题。
  - **运维与配置**: 更新了 `docker-compose` 及各环境配置文件，优化了数据库连接池与系统日志输出策略。

## 2026-02-01 (v2.27.1)

- **数据库架构文档标准化与初始化 SQL 重构**:
  - **初始化脚本重构**: 将 `数据库表归档.md` 中所有零散的 `ALTER TABLE` 和索引语句合并至初始 `CREATE TABLE`，形成可一键执行的数据库初始化归档。
  - **数据类型演进**: 全面采用 PostgreSQL 现代标准，包括 `bigint generated always as identity` 主键、`timestamptz` 时区感知时间戳以及 `jsonb` 高性能 JSON 存储。
  - **Schema 审计与一致性验证**: 使用 PostgreSQL MCP 工具对实时数据库进行全量审计，确保归档文档与生产环境字段 100% 对齐，并修正了 `user_platform_setting` 等表的字段命名差异。
  - **文档结构优化**: 重构 Markdown 文档标题，实现“标题 == 表名备注”的自动化映射，并对角色权限相关表进行了解耦展示。

## 2026-01-30 (v2.27.0)

- **多平台配置架构重构 (UserPlatformSettings)**:
  - **核心模型升级**: 将原有的 `UserBuffSettings` 升级为 `UserPlatformSettings`，实现了从单一平台配置向多平台（Buff/C5/Steam）通用配置架构的跨越。
  - **后端解耦**: 删除了过时的 `UserBuffSettings` 相关 Controller/Service/Mapper/Entity，全面切换至 `UserPlatformSettings` 实现。
  - **全栈接口同步**: 更新了前端 `settings.ts` API 调用与 `Settings.vue` 组件，确保设置项与后端新模型完全对齐。
  - **数据兼容性**: 修正了 `BuffScanTask` 等多个实体类中的关联逻辑，确保多平台环境下账号与风控配置的正确引用。

## 2026-01-30 (v2.26.2)

- **任务列表 UI 美化与布局优化**:
  - **时间格式统一**: 引入 `dayjs` 库，将任务列表的创建时间与完成时间从 ISO 格式美化为 `YYYY-MM-DD HH:mm:ss`，并增强了空值处理。
  - **操作列布局对齐**: 实现了“表头居中 + 内容左对齐”的视觉效果。通过自定义插槽将操作按钮改为左对齐，同时保持表头文字居中，提升了操作确定感。

## 2026-01-30 (v2.26.1)

- **C5 平台全链路优化与 Bug 修复**:
  - **任务调度稳定性增强**: 修复了 `C5TaskScheduler` 在任务达成目标后，因先停止线程导致 JDBC 写入被中断，从而无法正确持久化“已完成”状态的问题。引入“持久化优先”策略，确保状态落地后再清理资源。
  - **策略定价模型优化**: `C5TradeStrategyImpl` 引入了 Market Depth (市场深度) 锚点定价逻辑，脱离对 Buff 参考价的依赖，实现 C5 平台原生的动态买入价计算。
  - **全栈模型对齐**: 完善了 C5 SDK 中的资产 (`C5AssetInfo`) 与商品 (`C5ItemInfo`) 数据模型，修复了批量下单响应解析时的字段缺失问题。
  - **前端适配升级**: 针对 C5 平台特性优化了任务列表与配置界面，支持按平台维度切换任务展示逻辑，提升了跨平台操作的一致性。

## 2026-01-29 (v2.26.0)

- **工程规范与数据库变更 SOP 升级**:
  - **数据库审计与设计规范**: 引入了基于 PostgreSQL MCP 的表结构审计流程。规定在涉及数据变更前，必须强制使用 `mcp_PostgreSQL_query` 审计现有模型，并结合 `postgresql-table-design` 技能获取架构建议。
  - **全栈接口一致性守卫**: 新增前后端接口同步修改规范。强制要求在变更 API 时，必须同步更新后端 DTO/Controller 与前端 API 调用/Type 定义，防止双端脱节。
  - **安全编译约束**: 建立了“仅编译，不运行”的开发红线。修改后端代码后必须执行编译验证并修复 Bug，但严禁私自启动服务，以规避生产环境风控风险。
  - **代码复用准则**: 强化了在编写新业务逻辑前进行全局常量与枚举类搜索的复用要求，减少重复代码。

## 2026-01-29 (v2.25.0)

- **任务状态回调重构 (异步化)**:
  - **架构升级**: 将任务状态回调机制从 HTTP 同步请求重构为 Redis 异步队列 (`niro:queue:task:status`)，提升系统鲁棒性，防止后端重启导致的状态丢失。
  - **后端消费者**: 新增 `TaskStatusConsumer`，基于 `StringRedisTemplate` 和单线程执行器实现阻塞式消费，确保状态更新的有序性与可靠性。
  - **服务幂等性**: 增强 `BuffScanTaskService.taskCallback` 逻辑，引入状态机检查与幂等性控制，防止重复回调导致的状态异常。
  - **爬虫端适配**: 同步更新 `sharded_executor.py` 与 `async_buff_spider.py`，移除 `httpx` 回调逻辑，改为直接推送 Redis 队列。

## 2026-01-29 (v2.24.9)

- **数据库兼容性与持久化修复**:
  - **PostgreSQL JSON 类型转换修复**: 修复了在测试环境下执行数据库写入时出现的 `PSQLException: column "tags" is of type json but expression is of type character varying` 错误。
  - **类型处理器实现**: 新增了 `PostgresJsonTypeHandler`，通过显式使用 `PGobject` 声明 JSON 类型，解决了 MyBatis-Plus 默认 `JacksonTypeHandler` 在 PostgreSQL 环境下的兼容性问题。
  - **实体映射优化**: 更新了 `BuffGoods` 与 `TradeOrderRecord` 实体类，将 JSON/Map 类型的字段（如 `tags`, `extraInfo`）统一切换至自定义的 PostgreSQL 专用类型处理器。

## 2026-01-29 (v2.24.8)

- **运维与监控增强**:
  - **网络状态可视化**: 在 Python 爬虫启动日志中新增了代理状态 (`Proxy Enabled/Disabled`) 及当前出口 IP 的实时检测打印，便于运维人员快速确认网络环境（如 Clash/VPN 是否生效）。
  - **日志路径兼容性修复**: 修复了 `LogController` 在 Docker 环境下无法定位日志文件的问题，增加了对绝对路径配置 (`/app/logs/...`) 的优先支持。
  - **代理配置自动补全**: 修复了 `httpx` 库因代理 URL 缺失协议头 (`http://`) 而抛出 `Unknown scheme` 错误的问题，现在 `proxy_helper.py` 会自动补全协议前缀。
  - **分片执行器代理修复**: 修复了 `ShardedSpiderExecutor` 在执行分类同步、商品同步及印花同步任务时，未正确继承全局代理配置导致请求直连的问题。现在当账号未配置独立代理时，将自动降级使用全局代理。
  - **依赖更新**: 新增 `httpx[http2]` 依赖，以支持强制 HTTP/2 请求，解决因缺少 `h2` 库导致的 `ImportError`。

## 2026-01-28 (v2.24.7)

- **任务状态与数据完整性修复**:
  - **前端状态映射修正**: 修复了前端任务列表将“运行中”状态误显示为“待运行”的问题，确保界面准确反映后端任务的实时状态。
  - **商品元数据修复**: 修复了 Java 后端 `BuffGoodsReportDTO` 缺失 `internal_name` 字段导致的数据入库丢失问题；同步完善了 Python 爬虫端的兜底获取逻辑，确保在 API 标签缺失时仍能正确保存内部标识。
- **爬虫容器化与稳定性优化**:
  - **Dockerfile 重构**: 针对 Python 爬虫模块进行了 Dockerfile 深度重构，移除废弃的数据库依赖，适配异步消息驱动架构 (`main.py`)，并优化了时区与国内镜像源配置。
  - **核心 Bug 修复**: 修复了 `sharded_executor.py` 中因缺失 `Deque` 类型导入导致的 `NameError` 崩溃，确保分片执行器在印花同步任务中的稳定性。

## 2026-01-28 (v2.24.6)

- **C5 平台交易策略完善与修复**:
  - **核心逻辑修复**: 修复了 `C5TradeStrategyImpl` 中的类型转换错误（UserBuffSettingsDTO 误用作 Entity）及字符编码导致的中文乱码问题。
  - **余额同步实现**: 完成了 `syncAccountBalance` 接口实现，支持通过 C5 SDK 实时同步账号余额及状态，确保交易策略的资金校验准确性。
  - **数据模型增强**: 在 `TradeOrderRecord` 实体中补全了缺失的 `goodsId` 字段，解决了 C5 批量下单流程中的字段引用报错。
- **系统编译与启动优化**:
  - **Lombok 配置清理**: 移除了 `niro-web` 模块中冗余且冲突的 `maven-compiler-plugin` 配置，恢复了全局统一的注解处理器路径，彻底解决了 Getter/Setter 缺失导致的编译失败。
  - **后端启动验证**: 成功在 JDK 21 环境下完成全模块编译与服务启动，验证了数据库连接池、Redis 订阅及任务调度器的初始化流程。

## 2026-01-26 (v2.24.5)

- **系统稳定性与爬虫优化**:
  - **Redisson 组件升级**: 将 Redisson 版本从 `3.44.0` 升级至 `3.45.0`，修复了 Netty 线程中偶发的 `PubSubEntry.getQueue()` 空指针异常 (NPE)，提升了 Redis 消息订阅的稳定性。
  - **分类同步优化**: 针对“其他” (Other) 分类，在 `sharded_executor.py` 中禁用了连续 3 页无新数据即停止的提前结束优化，确保高度重复标签下的分类树能完整同步；同时增强了同步失败时的堆栈日志记录。

## 2026-01-24 (v2.24.4)

- **通知时长计算修复**:
  - **时长逻辑修正**: 修复了任务结束通知中“运行时长”显示异常（如显示数天）的问题。通过在任务启动、更新配置及状态变更时显式重置 `updateTime`，确保时长计算基准（开始时间）的准确性。

## 2026-01-23 (v2.24.3)

- **爬虫数据上报与监控修复**:
  - **数据入库修复**: 修复了 `niro-server` 在消费爬虫数据时，因 Hutool JSONObject 与 Jackson 序列化冲突导致的 PostgreSQL 数据入库失败问题（空表问题修复）。
  - **Redis 锁泄露修复**: 修复了 `get_buff_goods.py` 在推送数据到 Redis 队列时未正确释放锁导致的死锁风险，引入 `try...finally` 块确保资源释放。
  - **RPM 监控优化**: 优化了爬虫分片执行器的性能监控指标，将瞬时 TPS (Transactions Per Second) 升级为 60秒滑动窗口的 RPM (Requests Per Minute)，提供更平滑准确的速率展示。
  - **异常处理增强**: 修复了 `sharded_executor.py` 中的 `NameError` 变量未定义错误，并处理了数据保存函数返回 0 时的逻辑分支，防止静默数据丢失。

## 2026-01-23 (v2.24.2)

- **系统任务分片修复**:
  - **分片下发逻辑修正**: 修复了 `BuffTradeStrategyImpl` 中在创建系统任务（如商品同步）时，未将计算出的 `categoryIds` 传递给消息队列的问题。
  - **元数据缺失修复**: 修复了系统任务 Payload 中缺失 `categoryMeta` 导致爬虫端因无法获取 `internalName` 而跳过抓取的问题。现在后端会在下发任务时自动查询并封装分类的元数据（名称、内部标识等）。
  - **空分片告警**: 在后端分片下发逻辑中新增了日志监控，当分片计算结果为空时会输出警告日志，便于快速定位问题。

## 2026-01-23 (v2.24.1)

- **系统任务断点续传**:
  - **进度持久化**: 在 `sharded_executor.py` 中引入 Redis 进度记录机制。每当 Worker 完成一个分类抓取，即实时将 `category_id` 写入 Redis Set 并续期 7 天。
  - **智能续传**: 任务启动时自动检测是否存在历史进度 Key。若存在，自动过滤已完成的分类，仅对剩余分类进行补抓，彻底解决了因意外中断（重启/网络故障）导致的大型任务重复执行问题。

## 2026-01-23 (v2.24.0)

- **爬虫分片执行器深度优化 (Phase 2)**:
  - **并行消费架构**: 完成了 `sharded_executor.py` 的生产者-消费者模式改造。引入 `asyncio.Queue` 解耦任务分发与执行，支持单个账号下的多 Worker 并发抓取 (默认并发数 3)，显著提升了任务吞吐量。
  - **并发控制**: 使用 `asyncio.Semaphore` 实现精细化的并发限流，并配合动态休息机制，在提升效率的同时兼顾账号风控安全。
- **CSRF 自动修复增强**:
  - **预热机制**: 在 `async_buy_v3` 下单流程中新增了“预热逻辑”。在发起关键 POST 请求前，模拟人类行为访问市场首页并获取最新的 `Set-Cookie`，彻底解决了因 Token 过期导致的首次请求死锁问题。
  - **Cookie 策略优化**: 升级 `BrowserProfile` 的 Cookie 更新算法，确保在合并 Cookie 时遵循 Last-Write-Wins 原则，优先使用服务端下发的最新凭证。

## 2026-01-23 (v2.23.0)

- **爬虫分片执行器深度优化 (Phase 1)**:
  - **智能重试机制**: 在 `sharded_executor.py` 核心抓取链路引入 `tenacity` 装饰器，针对 `Timeout` 和 `ConnectionError` 等网络异常实现指数退避重试 (Exponential Backoff)，显著提升弱网环境下的任务成功率。
  - **死信队列 (DLQ)**: 构建了基于 Redis 的死信队列机制。当分类抓取重试耗尽后，自动将失败的 `category_id` 隔离至 `failed_categories` 队列并触发 Critical 告警，防止单点故障阻塞整体任务，保障数据完整性。

## 2026-01-23 (v2.22.1)

- **系统稳定性修复**:
  - **任务配置前端修复**: 修复了编辑任务时因 `accountIds` 数据为空导致的多选组件渲染异常 (`Cannot read properties of null`)。
  - **数据库解耦**: 物理移除了 Python 爬虫端的数据库依赖模块 (`storage/database.py`, `storage/models.py`)，彻底转向基于 Redis 的无状态架构。
  - **数据存储规范**: 移除了 `buff_goods` 表中的非元数据字段（如 `sell_min_price`, `sell_num` 等），确保商品表仅存储纯净元数据。

## 2026-01-22 (v2.22.0)

- **多平台交易策略架构升级**:
  - **策略模式重构**: 引入 `PlatformStrategyFactory` 及其对应的策略实现类，实现交易逻辑与具体平台的解耦，支持 BUFF 和 C5 平台。
  - **平台参数支持**: 在 `BuffScanTaskParam` 和 `BuffAccountDTO` 中新增 `platform` 字段，默认支持 "BUFF" 并预留 "C5" 扩展。
  - **统一配置扩展**: 引入 `apiConfig` 和 `extraConfig` 字段，支持不同平台个性化 API 配置（如 C5 的 API Key/Secret）。
- **C5 平台初步集成**:
  - **SDK 深度整合**: 整合 `niro-sdk` 模块中的 `C5ApiClient`，实现 C5 平台的任务下发骨架及策略分发验证。
- **系统稳定性修复**:
  - **编译与依赖修复**: 修复了多处因代码重构导致的 Import 缺失、方法名不匹配及 Lombok 注解处理器配置问题，确保后端服务正常启动。
  - **Redis 工具类优化**: 修复 `RedisUtil` 中的重复方法定义，提升系统稳定性。

## 2026-01-22 (v2.21.1)

- **C5 SDK 架构升级与性能优化**:
  - **模块化拆分**: 将庞大的 `C5ApiClient` 重构为 `Account`, `Market`, `Trade`, `Purchase`, `Inventory` 五大独立子模块，实现关注点分离。
  - **虚拟线程引擎**: 全新设计 `C5HttpEngine` 核心组件，底层采用 JDK 21 `HttpClient` 并配置 `Executors.newVirtualThreadPerTaskExecutor()`，充分利用虚拟线程优势处理高并发 I/O。
  - **连接池管理**: 摒弃短连接模式，原生支持 HTTP/2 与长连接复用，大幅降低网络握手开销。
  - **JSON 性能提升**: 将序列化层从 Hutool 迁移至 Jackson，进一步提升数据编解码效率。

## 2026-01-22 (v2.21.0)

- **第三方 SDK 模块 (niro-sdk)**:
  - **模块化架构**: 新增独立 Maven 模块 `niro-sdk`，解耦第三方平台对接逻辑，引入 Hutool、Jackson 等核心依赖。
  - **C5 开放平台接入**:
    - **核心组件**: 实现 `C5ApiClient` 统一客户端、`C5Config` 配置类及 `C5ApiException` 异常体系。
    - **全接口覆盖**: 完整对接 C5 开放平台 17 个核心接口，涵盖账户查询、饰品搜索、求购管理、库存查询及交易下单等全链路功能。
    - **模型封装**: 基于官方文档深度还原 `C5AssetInfo`、`C5ItemInfo` 等复杂数据模型，支持印花、磨损度、涂装种子等高精度字段解析。

## 2026-01-22 (v2.20.0)

- **扫货任务可靠性与监控增强**:
  - **CSRF 校验自愈**: 实现了 Cookie 自动同步与全链路 CSRF 自动重试机制。当检测到跨环境或 Token 过期导致的校验失败时，爬虫将自动捕获新 Token 并立即重试，确保下单流程不中断。
  - **RPM 实时监控**: 在扫描任务日志中新增了 RPM (Requests Per Minute) 实时统计，基于最近 1分钟的滑动窗口精准计算单账号的请求频率，直观反映负载情况。
  - **支付方式预过滤**: 在商品扫描阶段新增了支付方式匹配逻辑。若挂单明确不支持用户配置的支付方式（如仅限微信/支付宝），系统将自动过滤该商品，从源头上避免下单失败。
  - **心跳容错阈值调整**: 将任务间的心跳超时判定阈值从 60秒 延长至 120秒，有效减少因网络波动或 Python 引擎重启导致的“目标终端离线”误报。

## 2026-01-22 (v2.19.0)

- **商品列表与任务配置协同增强**:
  - **快捷扫货功能**: 在商品列表新增“扫货”快捷入口，支持一键跳转并预填当前商品信息，显著提升任务创建效率。
  - **表单布局优化**: 深度压缩任务配置弹窗的行间距与内边距，优化垂直空间利用率，解决小屏幕下的滚动条遮挡问题。
- **系统稳定性与性能修复**:
  - **依赖循环修复**: 通过引入 `@Lazy` 延迟加载机制，彻底解决了 `EmailNotifyService` 与 `UserBuffSettingsService` 之间的构造函数循环依赖问题，确保后端服务正常启动。
  - **前端异常修复**: 修复了点击“扫货”按钮时因 `openWithGoods` 方法未暴露导致的 `TypeError` 崩溃。
  - **编译问题修正**: 修复了 `BuffScanTaskService` 中因代码拼写错误导致的 Maven 编译失败。

## 2026-01-22 (v2.18.0)

- **账号指纹与安全性增强**:
  - **User-Agent 随机池**: 引入 `UserAgentConstant` 常量池，内置 10 款主流浏览器 UA 指纹。
  - **自动补全机制**: 优化账号状态检查逻辑，当账号 UA 为空时自动从池中随机分配并持久化，提升账号模拟的真实性。
  - **依赖修复**: 修复了 Web 模块因缺失 Core 模块依赖导致的 `NoClassDefFoundError` 问题。
- **任务配置中心体验修复**:
  - **单位转换修正**: 修复了扫描间隔最大时间在编辑保存后数值异常（60秒变1分显示为1秒）的问题，统一了时间单位转换逻辑。
  - **关联数据保持**: 修复了编辑任务时“关联下单任务”字段因监听器误触发而被清空的问题，引入 `nextTick` 确保数据回显正确。
- **实时日志系统稳定性修复**:
  - **鉴权白名单增强**: 修复了 `/log/stream` 接口因 Sa-Token 上下文未初始化导致的拦截异常。通过将排除逻辑移至拦截器注册层级，彻底解决了实时日志流被误拦截的问题。

## 2026-01-22 (v2.17.1)

- **账号列表显示优化**:
  - **待结算余额显示**: 优化了个人配置界面的余额显示逻辑。修复了当账号或总资产的“待结算金额”为 0 时被隐藏的问题，现在将明确显示为 `¥0.00`，确保资产数据的完整呈现。
  - **资产接口参数补全**: 修复了后端获取资产信息时缺失 `with_pending_divide_amount=1` 参数的问题，确保能正确获取到账号的待结算金额。
- **账号指纹增强**:
  - **随机 User-Agent**: 新增 `UserAgentConstant` 常量池，内置主流浏览器（Chrome/Edge/Firefox/Safari 等）的 UA 字符串。
  - **自动绑定**: 创建账号或刷新账号状态时，若未指定 User-Agent，系统将自动从常量池中随机选择一个进行绑定，并应用于后续的 Cookie 检测与余额更新请求中，提升账号模拟的真实性。

## 2026-01-22 (v2.17.0)

- **SSE 日志架构升级**:
  - **Redis 实时推送**: 后端 SSE 接口重构为 Redis Pub/Sub 模式，Python 端日志增加异步 Sink，移除了对日志文件的轮询读取，显著降低磁盘 I/O。
  - **初始快照优化**: 仅在连接建立瞬间读取日志文件末尾 100 行作为初始数据，随后无缝切换至 Redis 实时流。
  - **资源自动释放**: 实现 SSE 连接断开时的 Redis 订阅自动取消机制，防止资源泄漏。
  - **任务自愈策略调整**: 将任务心跳超时判定的阈值从 5 分钟缩短至 2 分钟，加快了 Python 引擎重启后任务自动恢复的速度。
  - **响应式架构重构**: 引入 `Spring WebFlux` 和 `Reactive Redis`，将日志推送接口升级为全异步响应式流 (`Flux`)，彻底释放了 Servlet 线程池资源，大幅提升高并发场景下的吞吐量与稳定性。

## 2026-01-22 (v2.16.1)

- **日志系统健壮性与交互体验修复**:
  - **修复日志白屏崩溃**: 修复了 `parseLog` 函数中的 JS 运算符优先级漏洞。通过添加显式括号，解决了在解析新旧格式混合日志时可能触发的 `TypeError: Cannot read properties of null (reading '1')` 报错，确保了日志流在高频推送下的稳定性。
  - **恢复系统级日志显示**: 优化了 `Logs.vue` 的标签过滤逻辑。不再强制隐藏 `System` 账号标签，确保任务启动、连接通知等全局性日志在 UI 上能够被正确归类并显示。
  - **点击过滤功能回归**: 彻底修复了标签点击失效的问题。重构了 `Logs.vue` 的正则解析逻辑，使其能够完美识别并提取标准 6 段式日志中的 IP、TraceId 和账号字段，并将其重新绑定至搜索框过滤事件。
  - **标签视觉优化**:
    - **清晰度提升**: 将 IP、TraceId、账号等点击标签的字体大小从 `10px` 微调至 `11px`，并增加了颜色对比度，解决了暗色背景下的小字模糊问题。
    - **美观度平衡**: 移除了标签的强制加粗效果，恢复为 `font-medium` 字重，使界面在保持清晰的同时更加精致和平衡。

## 2026-01-22 (v2.16.0)

- **日志系统标准化与全链路上下文优化**:
  - **结构化日志输出**: 深度重构日志格式，将 `IP`、`TraceId` 和 `账号名称` 提升为一级字段，实现 `时间 | 级别 | IP | TraceId | 账号 | 日志详情` 的标准输出结构，极大提升了多账号并发下的日志检索效率。
  - **日志内容瘦身**: 全量清理了爬虫模块（含异步下单、分片执行器、同步任务等）中冗余的手动 `[账号: xxx]` 前缀，消除信息重复展示，使日志更加简洁。
  - **上下文自动传递**: 优化了异步任务的 `ContextVar` 管理，确保账号信息（AccountName/ID）能够稳定穿透异步链路并自动注入日志，解决了部分高并发场景下账号字段缺失的问题。

## 2026-01-22 (v2.15.0)

- **日志系统深度重构与 UI 增强**:
  - **日志流瘦身**: 禁用了冗余的前端专用 JSON 日志生成，仅保留纯文本 `.log` (用于 UI 实时显示) 和结构化 `_efk.json` (用于生产级采集)，显著降低磁盘 IO。
  - **UI 实时看板升级**:
    - **标签化显示**: 日志条目新增 IP 和 TraceId 显式标签（绿/紫配色），支持点击 TraceId 快速过滤链路。
    - **智能解析引擎**: 前端实现正则解析，能够从纯文本行中自动提取时间戳、级别、IP 和追踪 ID。
    - **呼吸灯动画**: 为“下单步骤”日志新增蓝色呼吸灯背景特效，直观追踪购买进度。
    - **视觉增强**: 优化了日志级别（ERROR/WARN/INFO）的颜色与字体加粗效果，隐藏了冗余的类名路径。
  - **全链路监控闭环**:
    - **异常实时捕获**: 提升了下单失败、余额不足等业务报错的日志级别至 ERROR，确保能被前端“实时异常监控”窗口捕获。
    - **SSE 链路对齐**: 后端 LogController 适配了新的 `.log` 路径，并对齐了连接成功消息格式。
- **爬虫协议与兼容性修复**:
  - **CSRF 提取优化**: 增强了 `_get_csrf_token` 正则，支持带引号的 `csrftoken` 提取，并调整了 Cookie 键名优先级。
  - **指纹同步更新**: 将浏览器指纹从 Chrome/144 回退至 Chrome/143 以对齐用户实际环境，并补全了 `Priority` 和 `Accept-Language` 等关键 Header。
  - **Referer 动态生成**: 下单相关请求现在会动态生成对应的商品详情页 Referer，完美模拟真人点击路径。

## 2026-01-21 (v2.14.6)

- **CSRF 提取与注入逻辑终极对齐**:
  - **同步/异步逻辑对齐**: 统一了 `buff_spider.py` 和 `async_buff_spider.py` 中的 `_get_csrf_token` 提取逻辑，现在均支持 `csrf_token` 和 `csrftoken` 两种 Cookie 格式，提升了兼容性。
  - **Header 优先级修复**: 修正了基础请求类中的 Header 合并逻辑。确保手动传入的自定义 Header（如小写 `x-csrftoken`）具有最高优先级，不会被基础指纹中的默认 Header 覆盖。
  - **全链路 Header 标准化**: 在同步版的 `buy_v3` 最终确认请求中也补全了 `Origin`、`X-Requested-With` 等关键头部，确保全模式下单链路的一致性。

## 2026-01-21 (v2.14.5)

- **深度修复 CSRF 校验失败**:
  - **请求链路一致性**: 修复了 `_batch_buy_preview` 接口直接使用 `httpx` 而绕过主请求逻辑的问题，确保了代理服务器（Proxy）与主任务保持 100% 一致。这是解决由于 IP 切换导致 CSRF Token 失效的关键。
  - **协议完整性补全**: 在所有关键 POST 接口（预览、创建、下单、通知）中补全了 `Origin` 和 `X-Requested-With` 头部，并将 CSRF Header 统一为官方标准的 `x-csrftoken`。
  - **核心请求器升级**: 重构了 `_request` 基础方法，支持 `return_raw` 模式，在保持自动重试和代理切换优势的同时，允许获取 `buff-cashier-trace-id` 等关键响应头。

## 2026-01-21 (v2.14.4)

- **下单协议深度优化 (对齐官方 JSON)**:
  - **修复 CSRF 校验失败**: 针对 `batch_buy/preview` 接口出现的 `CSRF Verification Error`，将 Header 键名对齐官方修改为小写 `x-csrftoken`，并补全了 `X-Requested-With: XMLHttpRequest` 头部。
  - **增强 Token 提取**: 优化了正则提取逻辑，兼容 `csrf_token` 与 `csrftoken` 两种 Cookie 键名。
  - **对齐批量下单链路**: 引入了 `batch_id` 随机生成机制，并在批量下单完成后改用 `bill_order/notify_buyer_to_send_offer` 接口进行统一通知，完美模拟真人浏览器操作流程。

## 2026-01-21 (v2.14.3)

- **UI/UX 体验优化**:
  - **动态显示购买数量**: 修复了扫描任务在关联下单任务时隐藏“购买数量”输入框的问题。现在当扫描任务绑定了目标下单任务，或任务模式为“仅下单”时，系统将自动显示购买数量配置，确保分布式任务的配额可控。

## 2026-01-21 (v2.14.2)

- **日志国际化与一致性优化**:
  - **中文化日志输出**: 将扫描任务跳过、离线告警等核心日志从英文改为中文，提升国内用户的使用体验。
  - **标识符统一**: 将日志中的 `[Task-ID: xxx]` 统一替换为更符合直觉的 `[任务ID: xxx]`。

## 2026-01-21 (v2.14.2)

- **任务安全与一致性增强**:
  - **任务删除拦截**: 在删除任务时增加关联性检查。若下单任务已被其他扫描任务绑定为“目标下单任务”，系统将自动拦截并报错，防止因误删导致扫描端路由失效。

## 2026-01-21 (v2.14.1)

- **任务协同与心跳机制优化**:
  - **心跳零延迟启动**: 任务启动后立即发送 Redis 心跳，解决扫描端初始化过快导致误判下单端离线的问题。
  - **检测容错增强**: `_process_items` 引入双重在线校验与详细的离线原因日志，提升多任务编排下的稳定性。

## 2026-01-21 (v2.14.0)

- **下单全链路闭环与自动支付策略优化**:
  - **支付方式对齐与扩展**: Python 爬虫端与 Java 后端同步引入 `BUFF_BALANCE` (BUFF余额支付, ID: 59) 枚举，对齐官方收银台协议。
  - **智能支付路由 (Fallback Mechanism)**: 实现下单前余额实时校验。若用户选择 `BUFF_BALANCE` 且余额不足，系统将自动平滑切换至 `BALANCE` (网易支付) 进行尝试，确保下单成功率。
  - **官方支付流对齐 (Protocol Alignment)**:
    - **支付通知补全**: 下单成功后自动调用 `notify_buyer_to_send_offer` 接口，模拟官方支付成功后的卖家通知行为，缩短发货等待时间。
    - **真人行为增强**: 优化了支付方式描述字段，并严格遵循官方请求链路顺序。
  - **实时资产看板 (Balance Sync)**:
    - **自动刷新**: 每次下单完成后，爬虫自动抓取账号最新余额（现金/待结算）。
    - **异步上报**: 引入 `reportAccountInfo` 接口，将最新资产状况实时同步至后端数据库，实现前端看板的动态更新。
  - **前端交互升级**: `Settings` 页面新增 BUFF 余额支付选项，并将原“余额”支付重命名为更直观的“网易支付”。

## 2026-01-21 (v2.13.0)

- **任务配置交互与校验深度优化**:
  - **校验机制重构**: 表单校验触发时机统一调整为“提交时校验”，彻底解决弹窗开启即报错的问题，提升交互流畅度。
  - **布局与提示优化**: 精细化调整表单元素间距，确保错误提示（Error Message）在紧凑布局下不被遮挡，并修复了特定字段下报错信息溢出背景框的 UI 缺陷。
  - **字段精简**: 移除 `listenerTag` 字段及相关逻辑，进一步简化任务配置流程。
  - **状态管理增强**: 弹窗关闭后强制销毁 DOM (destroy-on-close)，并引入 `nextTick` 与 `clearValidate` 确保每次打开均处于干净状态。

## 2026-01-21 (v2.12.0)

- **下单任务逻辑优化**:
  - **商品绑定增强**: 新建下单任务时支持选择关联商品，任务名称将自动根据商品名生成（格式：`商品名 (下单:ID)`），提升任务辨识度。
  - **智能路由过滤**: 扫描任务在关联下单任务时，下拉列表将根据当前选中的商品进行精确过滤，仅展示相同商品的下单任务，确保路由逻辑的正确性。
  - **交互体验升级**: 调整任务配置表单布局，引入字段间依赖校验，引导用户先选商品再关联下单任务。

## 2026-01-21 (v2.11.0)

- **任务配置逻辑精简**:
  - **移除关联下单账号**: 全面移除 SCAN 模式下的“关联下单账号”配置及相关逻辑，统一通过“关联下单任务”进行跨任务路由，简化操作路径。
  - **数据库字段清理**: 移除 `buff_scan_task` 表中的 `target_trade_account_id` 冗余字段。

## 2026-01-21 (v2.10.0)

- **任务运行模式与任务编排升级**:
  - **新增运行模式**: 引入 SCAN/TRADE/BOTH，支持“仅扫描/仅下单/全能模式”，下单任务独立监听队列。
  - **下单路由与账号绑定**: 扫描任务支持关联下单任务/下单账号，命中后按目标任务路由或本地指派账号执行。
  - **系统任务统一管理**: 新增系统任务类型与后台校验，支持分类/商品/印花等同步任务的统一管理。
- **任务管理界面重构**:
  - **独立任务列表**: 新增任务管理页，按扫描/下单/系统任务分 Tab 管理。
  - **配置面板升级**: 任务配置新增运行模式、关联下单任务、账号角色过滤与计划配置优化。

## 2026-01-21 (v2.9.4)

- **扫货任务：补齐倒卖模式利润判定与定价参考**:
  - **利润筛选闭环**: Python 端支持 `FLIPPING` 任务按 `minProfit` 计算净利润，筛选命中后再进入下单路由或本地撮合。
  - **价格参考补全**: 异步扫描结果注入 `sell_min_price`/`buy_max_price`，确保倒卖模式具备稳定的参考价来源。

## 2026-01-21 (v2.9.3)

- **扫货任务：优化异步下单安全机制与修复批量下单异常**:
  - **引入“人类行为模拟”冷却**: 在 `async_buy_v3` 中新增了 `Sequential Guard` (下单间隔保护) 与 `Post-Order Cooldown` (强制余温冷却)，下单成功后强制休息 1-2 分钟，有效规避高频连打触发的脚本检测。
  - **加固确认逻辑**: 在最终确认阶段引入微秒级随机抖动，并强制重新获取 CSRF Token，彻底解决 `None` 响应导致的逻辑断裂。
  - **修复“价格改变”报错**: 最终确认阶段（`/api/market/goods/buy`）完全对齐 HAR 协议：
    - **单价对齐**: 修正 `price` 字段为饰品单价（`unit_price`）而非总价。
    - **字段精简**: 移除了批量模式下冗余且会导致报错的 `num` 字段。
    - **Headers 补全**: 移除了不该存在的 `buff-cashier-trace-id`，补全了 `X-Requested-With` 和 `Accept` 关键头信息。
  - **精细化参数类型**: 确认 `batch_buy/create` 接口中 `frozen_amount` 为数字，`max_price` 和 `num` 为字符串，已完成全链路类型对齐。
  - **补全预览参数**: 在 `batch_buy/preview` 阶段补全了 `select_epay: 1` 参数，对齐官方收银台协议。
  - **支付授权优化**: 自动识别需手动支付授权的场景，并将该账号的 `Busy` 锁自动延长至 180s，为人工干预预留充足时间。
  - **异常诊断增强**: 在最终确认失败时输出完整的请求 Payload 日志，便于快速定位协议适配问题。

## 2026-01-20 (v2.9.2)

- **扫货任务：架构级重构异步下单链路 (全异步化 & 稳定性增强)**:
  - **全异步 batch_buy_v3 实现**: 参照同步版逻辑，在 `AsyncBuffSpider` 中完整复刻了 [预览 -> 创建 -> 支付预处理 -> 轮询状态 -> 最终确认] 五步流程。使用 `httpx.AsyncClient` 确保全链路非阻塞。
  - **非阻塞任务调度**: 重构 `_process_items` 使用 `asyncio.create_task` 发起下单，主扫描循环不再等待下单结果，实现“秒级发现、秒级下单”的并发能力。
  - **补全批量下单核心字段**: 在 `async_buy_v3` 的最终确认步骤中，补全了 `num` (实际购买数量)、`hide_non_epay`、`batch_id` 等关键字段，确保批量下单请求参数与 BUFF 官方协议完全一致，提升下单成功率。
  - **修复 CSRF 校验异常**: 修复了 `_batch_buy_preview` 异步方法中缺失 `X-CSRFToken` 请求头导致的 `CSRF Verification Error` 报错，确保下单全链路鉴权一致。
  - **强化账号 Busy 感知**: 将账号 `Busy` 锁提升至 200s (TTL)，并在创建订单成功后立即锁定。引入 `Already Paying` 熔断机制，一旦触发立即标记 Busy 200s 并回滚饰品状态。对齐 BUFF 3分钟支付超时机制。
  - **支付轮询深度优化**: 将 `async_buy_v3` 的支付状态轮询时间由 60s 延长至 180s (3分钟)，确保在高并发支付拥堵时仍能完整追踪订单状态。同时新增了每 15s 一次的进度日志，解决长时间轮询导致的日志“消失”错觉。
  - **分布式锁生命周期优化**: 统一了 `_release_items` 逻辑。在预览失败、支付超时或最终确认失败等场景下，自动清理 Redis 中的 `PendingPurchase` 与 `Lock` 状态，确保捡漏机会能快速流转至其他备选账号。
  - **TraceID 链路追踪增强**: 确保异步预览请求能正确提取并传递 `buff-cashier-trace-id`，实现跨请求的完整业务追踪。

## 2026-01-20 (v2.9.1)

- **扫货任务：深度优化高并发下的下单可靠性与性能**:
  - **批量撮合升级**: 重构 `_process_items` 逻辑，在单次扫描周期内实现全量匹配项的聚合，强制使用 `batch_buy_v3` 异步批量接口，杜绝低效的单步循环调用。
  - **账号 Busy 状态感知**: 引入 Redis `Busy` 状态位。当 Buff 返回 `Already Paying` 限制时，立即将该账号标记为 Busy (30s)，确保在支付卡顿期间不占用多余请求。
  - **饰品重试队列 (Retry Queue)**: 实现基于 Redis List 的待重试队列。针对因账号 Busy 或预览冲突导致的下单失败，饰品 ID 将自动入队并保留分布式锁，待账号空闲后优先处理。
  - **日志聚合与脱敏**: 针对 Busy 状态及重复下单失败引入了 10s 级别的日志节流阀 (Throttle)，大幅精简了高并发场景下的冗余报错，保持日志窗口整洁。

## 2026-01-20 (v2.9.0)

- **扫货任务：新增异步批量下单机制 (捡漏提效)**:
  - **异步化迁移**: 在 `AsyncBuffSpider` 中实现了基于 `httpx` 的异步批量下单全链路逻辑，包含预览、创建、支付预处理及状态轮询。
  - **聚合下单逻辑**: 重构 `_process_items` 方法，支持将单次 API 响应中命中的多个低价饰品聚合，一次性发起批量下单请求。
  - **异常自愈能力**: 批量下单预览阶段若发现部分 ID 被抢占，系统会自动剔除失效 ID 并重新尝试剩余 ID 的下单，显著提升捡漏成功率。
  - **非阻塞轮询**: 使用 `asyncio.sleep` 实现支付状态轮询，确保在等待支付确认期间不阻塞其他分片账号的扫描任务。
  - **TraceID 继承**: 确保批量下单全链路（预览、创建、支付、最终确认）均携带同一个 TraceID，日志记录更清晰。

## 2026-01-20 (v2.8.3)

- **扫货任务：强化下单可靠性与修复崩溃问题**:
  - **修复请求参数冲突**: 解决了 `_request` 调用 `_do_request` 时因 `headers` 关键字重复传递导致的 `got multiple values for argument 'headers'` 错误。
  - **修复下单崩溃**: 修复了 `_create_order` 中因 `profile` 对象未初始化导致的崩溃。现在下单账号会根据 `buffCookie` 动态创建 `BrowserProfile`。
  - **下单逻辑激活**: 补全了异步下单方法，实现 CSRF Token 提取与 Buff 购买接口对接。
  - **精准下单去重**: 引入 Redis `PendingPurchase` 集合（TTL 60s），防止多账号/多协程重复下单。
  - **锁逻辑闭环优化**: 强化分布式锁管理，锁的释放严格依赖 API 返回结果。
  - **分片一致性均衡**: 优化分片算法，通过确定性排序确保监控目标互不重叠。
  - **日志精简提效**: 移除了高频扫描场景下的冗余日志，仅记录状态变更。

## 2026-01-20 (v2.8.2)

- **账号系统：优化 Cookie 失效判定逻辑**:
  - **增强检测精度**: 针对 Buff API 在 Cookie 失效时返回 HTML 重定向页面（Redirecting...）而非标准 JSON 的情况，在后端与爬虫端同步引入了 HTML 内容预检机制。
  - **全链路覆盖**: 修复了 Java 后端 `checkCookie` 检测逻辑，以及 Python 爬虫端同步（`BuffSpider`）与异步（`AsyncBuffSpider`）的所有请求入口。
  - **异常容错**: 明确了“非成功响应即失效”的原则，确保网络超时、JSON 解析失败或非 OK 业务码均能触发账号状态自动标记为 `INVALID`，提升了系统对风控及登录失效的感知灵敏度。

## 2026-01-20 (v2.8.1)

- **爬虫：优化日志可读性**:
  - **分类 ID 替换**: 在商品同步和清理的日志中，将原始的分类 ID 替换为可读性更强的分类名称（如 `[分类: 匕首]`），并优化了多账号并行时的日志归属展示。
  - **性能优化**: 实现了分类名称的按需异步查询与缓存透传，在提升日志质量的同时避免了对数据库的冗余访问。

## 2026-01-20 (v2.8.0)

- **同步器优化：基于版本标识的增量清理与多账号并行提速**:
  - **增量清理机制**: 引入基于时间戳的唯一同步版本标识 (`sync_tag`)。同步过程中实时打标，任务完成后自动异步清理分类下未匹配当前版本的过期商品数据，实现精准的下架感知。
  - **多账号并行分片**: 重构 `ShardedSpiderExecutor`，实现待同步分类在 `NORMAL` 账号间的均匀分配与并发抓取，显著提升大批量分类同步效率。
  - **Hash 跳过机制**: 集成 Redis Hash 校验。同步分类首页后比对商品 ID 列表哈希，若无变动则自动刷新版本标识并跳过后续页码，极大降低无效 API 请求。
  - **实时 TPS 监控**: 日志系统新增单账号级别的抓取 TPS (每秒处理商品数) 与分片进度展示，任务状态感知更细致。
  - **防御性编程**: 增强了对 429/401 错误的处理，发生风控异常时严禁执行清理逻辑以防误删，并触发任务自动重平衡。

## 2026-01-20 (v2.7.9)

- **系统优化：TraceId 缩短与体验提升**:
  - **生成算法升级**: 将全链路追踪 ID (TraceId) 的生成逻辑由 32 位 UUID 优化为 12 位 NanoId (Java) 和 URL-Safe 短 ID (Python)，在保证极低碰撞率的同时大幅提升了日志的可读性。
  - **存储与展示优化**: 缩短后的 ID 更利于在移动端通知、前端日志界面中展示，并略微减少了 Elasticsearch 索引的存储开销。

## 2026-01-20 (v2.7.8)

- **爬虫：修复多账号扫描准入控制异常**:
  - **修复 AttributeError**: 补全了 `AsyncBuffSpider` 类中缺失的 `_get_dynamic_wait_time` 方法，解决了多账号并发扫描时因方法未定义导致的执行中断。
  - **准入逻辑增强**: 重新集成了基于 Redis Lua 脚本的原子准入控制（Admission Control），确保多账号请求能够精准错峰，实现流量整形与均匀分布。

## 2026-01-20 (v2.7.7)

- **日志与通知体验深度优化**:
  - **样式修正**: 彻底移除了“发现捡漏机会”日志的斜体样式，通过 `not-italic` 强约束确保在不同终端下均保持标准加粗显示，提升阅读舒适度。
  - **信息补全**: 在企业微信卡片通知中补全了 `[账号: ]` 字段，确保多账号并发场景下能快速识别消息来源。
  - **描述对齐**: 统一将日志与通知中的“挂单ID”更名为“饰品ID”，与行业通用术语对齐，降低理解成本。

## 2026-01-20 (v2.7.6)

- **体验优化：重塑“发现捡漏机会”日志与通知格式**:
  - **日志样式调整**: 移除了前端日志界面中的斜体显示，统一改为加粗高亮，提升识别度。
  - **通知内容增强**: 在企业微信卡片通知的描述中新增了 `[账号: ]` 信息，方便多账号监控时快速定位。
  - **格式兼容性**: 确保所有“发现捡漏机会”相关的通知均采用卡片格式，规避 Markdown 兼容性问题。

## 2026-01-19 (v2.7.5)

- **爬虫：修复监控模式下的逻辑越权问题**:
  - **严谨的权限判定**: 统一了 `scan_task`、`_account_scan_loop` 与 `_process_items` 对“下单权限”的判断标准。现在系统仅依据账号配置的 `role` 角色（需为 `TRADE` 或 `BOTH`）来决定是否执行下单，彻底解决了未配置下单账号时依然尝试获取分布式锁并下单的逻辑缺陷。

## 2026-01-19 (v2.7.4)

- **爬虫：新增通知频率控制机制**:
  - **饰品报送去重**: 引入 Redis 缓存层，对监控模式下的同一饰品 ID 实现 3 分钟静默期。若相同饰品在 3 分钟内再次命中，将自动跳过日志打印与消息推送，大幅降低冗余通知干扰。
  - **分布式缓存支持**: 使用 `niro:report:cache:{item_id}` 作为键名，支持多账号并发环境下的全局统一去重。

## 2026-01-19 (v2.7.3)

- **爬虫：日志与通知体验优化**:
  - **日志格式重塑**: 移除了扫货匹配日志中的斜体样式，改为标准加粗显示，并统一增加了 `[账号: ]` 前缀，便于在多账号并发时精准追踪。
  - **通知形式升级**: 企业微信通知由 Markdown 消息切换为更直观的“文本卡片 (TextCard)”格式，解决了部分客户端 Markdown 兼容性问题，并提升了移动端的视觉呈现。

## 2026-01-19 (v2.7.2)

- **后端：修复扫货任务自愈逻辑编译错误**:
  - 补充了 `BuffScanTaskServiceImpl` 中缺失的 `BuffAccountRoleEnum` 枚举导入，解决了自愈任务在重新入队时因编译未通过导致的运行异常。
- **Python：全链路 TraceId 与 UserID 透传修复**:
  - **UserID 补全**: 修复了扫货任务消息中 `userId` 字段提取逻辑，确保企业微信通知能正确关联至所属用户，解决了日志中 `UserID: None` 的问题。
  - **字段名对齐**: 修复了 Java 与 Python 之间关于下单账号列表的字段名不一致问题（`execAccountIds` vs `buy_accounts`），确保扫货匹配后能正确触发下单逻辑。
- **Python：消息通知系统优化**:
  - 重构 `notifier.py`，移除了已废弃的数据库配置查询逻辑及其警告信息，提升了通知模块的执行效率与纯净度。

## 2026-01-19 (v2.7.1)

- **扫货任务“间歇式扫描”模式上线 (Anti-Risk Update)**:
  - **工作周期 (Duty Cycle) 逻辑**: 引入“运行-暂停”循环执行模式。任务在持续扫描指定时间（如 15 分钟）后，会自动进入强制休眠状态（如 5 分钟），有效规避 Buff 风控系统的 24 小时高频扫描检测。
  - **UI 模块深度重构**:
    - **模式切换**: “运行计划”模块新增“持续执行”与“周期循环”单选切换，界面布局更加直观。
    - **动态计划描述**: 新增动态文本提示，根据用户输入的运行时长与休眠时长，实时生成计划总结（如：“任务将每扫描 15 分钟后自动休息 5 分钟，循环执行直至手动停止”）。
    - **视觉交互增强**: 使用浅蓝色 Alert 样式容器包裹计划任务模块，增强视觉存在感，并对运行时间（蓝色）与暂停时间（橙色）进行颜色区分。
  - **全链路参数贯通**: 完成了从 Vue 前端配置、Java DTO 传输、Redis 消息队列下发到 Python 爬虫异步执行逻辑的全链路适配。
  - **UI 细节修复**:
    - **下拉框重叠修复**: 解决了任务编辑弹窗中，账号下拉框的账号名与已绑定任务文案相互重叠的 UI 冲突。
    - **表单校验优化**: 修复了打开编辑弹窗时，执行账号项在用户未操作前即显示报错信息的体验问题。
  - **Python 执行器稳定性修复**:
    - **Jackson 序列化解析增强**: 针对 Java 后端下发的消息中，数值字段（如 `maxPrice`, `durationMinutes`, `scanInterval`）和列表字段（如 `accounts`）因 Jackson 默认序列化带有的类型信息（如 `['java.lang.Integer', 15]`）导致的解析错误进行了全面修复。
    - **数值提取防御性逻辑**: 引入了 `_extract_value` 与 `_get_float` 工具方法，支持递归解包 Jackson 类型嵌套，并增加了数值转换的防御性处理，彻底解决了 `TypeError: float() argument must be a string or a real number, not 'list'` 问题。
    - **修复 BrowserProfile 实例化错误**: 修复了 `_account_scan_loop` 中手动实例化 `BrowserProfile` 时参数不匹配导致的执行中断（TypeError）。
    - **初始化异常捕获**: 为账号扫描子协程增加了全局 `try-except` 捕获，确保初始化阶段的异常能被记录到日志中，而非静默失败。

## 2026-01-19 (v2.7.0)

- **运行日志系统全面焕新与体验升级**:
  - **沉浸式 UI 布局**: 统一了日志界面与扫货管理的风格，移除了内边距并合并了工具栏与日志窗口，实现了全屏占满的沉浸式体验。修复了过滤模式下容器高度坍塌的问题。
  - **多关键字过滤 (AND 逻辑)**: 引入 `t-tag-input` 组件，支持多个关键字组合过滤，帮助精准定位复杂场景下的日志。
  - **实时高亮与追踪**:
    - **格式标准化**: 统一了日志中 TraceId 和 IP 的显示格式（如 `[TraceId: xxx]`、`[IP: xxx]`），与账号格式保持一致。
    - **交互增强**: 支持点击日志中的 TraceId 和 IP 标签，将其快速添加到过滤输入框中。
    - **视觉优化**: 针对不同类型的标签（账号、TraceId、IP）定制了区分化的色彩高亮，并优化了高对比度终端滚动条。
  - **系统健壮性优化**:
    - 针对 `t-tag-input` 交互增加了全量类型校验与空值防御，确保在关键字输入、删除或清空过程中界面的高度稳定性。
    - **修复 Java 控制台乱码**: 在 Maven 配置中强制指定 JVM 运行参数 `-Dfile.encoding=UTF-8`，彻底解决 Windows 环境下控制台输出中文乱码的问题。
    - **消息驱动可靠性增强**:
      - **启动前置校验**: Java 端启动任务时新增账号状态强校验，若无正常状态账号将直接报错提醒，避免任务“静默失败”。
      - **自愈响应加速**: 将任务监视器 `CategoryTaskMonitor` 的轮询间隔从 2 分钟缩短至 1 分钟，显著提升异常断线后的自动恢复速度。
      - **调试链路透明化**: Python 侧引入阻塞队列原始消息审计日志，支持通过运行日志追踪任务下发的全生命周期。
  - **爬虫依赖自愈**: 彻底修复了 Python 爬虫在执行异步任务时缺失 `asyncpg` 模块的问题，通过重新安装并同步环境，确保分类同步等异步数据库操作的稳定性。

## 2026-01-19 (v2.6.1)

- **Python 爬虫依赖修复**:
  - **缺失依赖补全**: 修复了在执行“同步分类树”任务时，因缺少 `asyncpg` 模块导致 PostgreSQL 异步写入失败的问题。已在 `niro-spider` 环境中完成依赖安装，并同步更新了 `requirements.txt`。

## 2026-01-19 (v2.6.0)

- **SSE 日志系统深度重构与体验升级**:
  - **多维度实时过滤**: 实现了不中断流的实时日志筛选功能，支持按账号、分类、关键字及“仅看错误”进行毫秒级过滤。
  - **全链路追踪联动**: 日志中的 `traceId` 现在支持点击快速过滤，实现单次抓取链路的秒级聚焦。
  - **高性能渲染优化**: 引入 500 行日志滚动淘汰机制，采用 `requestAnimationFrame` 优化大并发下的滚动流畅度。
  - **视觉噪声消除**: 移除了日志中冗余的 SSE 时间戳，并对账号名、分类 ID 等关键信息进行了色彩高亮。
- **系统健壮性与代码规范治理**:
  - **后端元数据校验**: 统一了 `categoryMeta` 的注入逻辑，并在任务下发前增加了无效分类 ID 的自动过滤，彻底解决“Payload 缺失元数据”导致的爬虫异常。
  - **全包名引用清理**: 按照阿里巴巴 Java 开发规范，清理了后端 Service 与 Controller 中的全路径类引用（如 `java.util.Map`），统一采用 Import 简写。
  - **爬虫异常详情增强**: 在分片执行器中增加了针对“处理分类失败”的完整堆栈日志捕获，极大提升了排障效率。
  - **前端布局优化**: 压缩了页面 Footer 空间，移除了内边距并适度放大字体，修复了日志窗口在极端情况下的溢出与滚动失效问题。

## 2026-01-19 (v2.5.1)

- **爬虫健壮性与稳定性修复**:
  - **空指针异常修复**: 修复了 `ShardedSpiderExecutor` 在解析任务 Payload 时，因 `categoryMeta` 缺失或为 null 导致的 `NoneType` 异常。
  - **死循环防御**: 针对 Payload 缺失分类元数据的情况，增加了自动跳过逻辑并标记为完成，防止执行器在错误数据下无限重试。
  - **执行流优化**: 在分片执行主循环中引入退避延迟，避免在极端错误场景下 CPU 占用过高。
  - **前端日志体验优化**: 实现了日志界面的自动滚动到底部功能，并为日志中的账号标识（Acc-xxx）增加了高亮显示。

## 2026-01-19 (v2.5.0)

- **任务生命周期管理与通知系统重构**:
  - **后端：任务状态管理核心化**:
    - **状态闭环逻辑**: 引入任务回调接口 `/task/callback/status`，统一由后端处理 Python 端的执行结果（完成/异常/停止），确保任务状态在数据库中的强一致性。
    - **优雅停机信号**: 实现基于 Redis 的任务停止信号机制 (`niro:task:stop:{id}`)。后端在点击停止时下发信号，Python 端异步感知并平滑退出。
    - **微信通知中台化**: 新增 `WeComNotifyService`，将微信通知逻辑从 Python 端迁移至后端。支持根据 `userId` 动态获取 `user_buff_settings` 表中的企业微信配置（Corpid, Secret, AgentId, ToUser），并具备基于 Corpid 的 AccessToken 缓存与全局兜底机制。现在任务的【启动】、【手动停止】、【自然完成】、【异常中断】均由后端统一发送。
  - **Python：执行器标准化与解耦**:
    - **停止信号感知**: `ShardedSpiderExecutor` 与 `AsyncBuffSpider` 均集成 Redis 停止信号轮询逻辑，支持任务在分片执行或循环扫描过程中秒级感知停机并安全释放资源。
    - **异步回调机制**: 任务结束（无论成功或失败）后，Python 执行器通过 HTTP 回调告知后端最终状态，彻底移除对数据库状态字段的直接写入。
    - **通知逻辑剥离**: 移除 Python 端冗余的 `notifier.py` 逻辑调用，使爬虫端回归纯粹的“执行器”角色。

## 2026-01-19 (v2.4.3)

- **异步架构演进与数据库解耦 (v2.4.0 关键补丁)**:
  - **异步数据库操作全面迁移**:
    - **UPSERT 逻辑异步化**: 重构 `get_buff_goods_category.py`、`get_buff_goods.py` 及 `buff_sticker_spider.py`，将所有同步数据库写入逻辑替换为 SQLAlchemy 异步引擎 (`async_session_factory`)，并实现高性能批量 UPSERT。
    - **印花计算器异步化**: `PremiumCalculator` 全面重构为异步模式，支持异步获取印花价格并内置缓存机制，消除扫描任务中的同步阻塞点。
  - **数据库查询硬性隔离**:
    - **任务状态解耦**: 严格遵守“Python 禁止查询数据库获取任务状态”指令，彻底移除 `cookie_util.py`、`notifier.py` 等工具类中的同步 DB 查询逻辑。
    - **上下文变量驱动**: 强制所有爬虫逻辑通过 `ContextVars` (如 `account_id_var`) 获取执行上下文，不再依赖数据库回退方案。
  - **基础架构增强**:
    - **异步 Redis 集中化**: `redis_pool.py` 新增 `redis_async` 全量支持，`ShardedSpiderExecutor` 现已完全切换至异步 Redis 客户端进行进度更新与分布式锁定。
    - **印花同步集成**: 将“印花价值同步”正式集成至异步分片执行器逻辑中（TaskType 4），支持多账号并发采集。
  - **Redis 键名对齐**: 统一 Python 与后端 Java 端的 Redis 键名前缀为 `niro:`，确保跨语言任务队列与统计数据的精确匹配。

## 2026-01-19 (v2.4.2)

- **后端任务分页查询修复**:
  - **Redis 序列化异常修复**: 修复了 `/task/page` 接口在补充任务实时统计信息（Stats）时，因直接读取 Python 写入的 JSON 字符串导致 Jackson 报错 `missing type id property '@class'` 的问题。统一使用 `RedisUtil.getToString` 进行无损读取，确保跨语言数据交互的稳定性。

## 2026-01-19 (v2.4.1)

- **爬虫数据同步健壮性修复**:
  - **SQLAlchemy 导入修复**: 修复了 `get_buff_goods_category.py` 中缺失 `sqlalchemy.func` 导入的问题，解决了分类同步任务在执行 UPSERT 操作（更新 `update_time`）时因 `NameError` 导致的任务中断。
- **后端 Redis 序列化异常修复**:
  - **跨语言序列化兼容**: 修复了 `BuffScanTaskServiceImpl` 在自愈逻辑中读取 Python 写入的 JSON 进度时，因 Jackson 缺少类型标识 (`@class`) 导致的 `SerializationException`。
  - **RedisUtil 增强**: `RedisUtil` 引入 `StringRedisTemplate`，优化 `getToString` 方法以支持纯字符串/跨语言 JSON 数据的无损读取。

## 2026-01-19 (v2.4.0)

- **基础数据分片采集与多账号协同重构**:
  - **后端 Java：分片下发与自愈守护**:
    - **Payload 结构升级**: 任务下发 Payload 现包含绑定的所有 NORMAL 账号列表、用户 ID 及分类分片 ID 列表。
    - **自愈守护 (Self-Healing)**: 实现 `CategoryTaskMonitor`，周期性检查运行中任务的 Redis 心跳，支持 Redis 宕机或 Python 崩溃后的断点续传与重平衡。
    - **账号动态注入**: 下发任务前强制过滤非 NORMAL 状态账号，确保执行效率。
  - **Python 协同执行器：异步分片执行**:
    - **ShardedSpiderExecutor**: 基于 `asyncio` 实现的全新执行器，支持分类/商品同步任务的动态分片映射与异步执行。
    - **频率控制与隔离上下文**: 集成 `aiolimiter` 实现账号级 QPS 限制，利用 `contextvars` 确保请求上下文严格隔离，日志输出包含账号标识。
    - **动态剔除逻辑**: 自动捕获 401/429 异常，实时调用 API 更新后端账号状态，并触发任务重平衡。
  - **存储层优化：幂等写入与冲突处理**:
    - **Upsert 机制**: 重构 `category` 与 `goods` 写入逻辑，采用 PostgreSQL `INSERT ... ON CONFLICT` 语法，解决并发冲突。
    - **分布式状态锁**: 在分类树与商品同步入库时引入 Redis 锁，防止多账号同时更新同一分类节点导致的数据不一致。
  - **实时监控：TPS 与 任务进度可视化**:
    - **状态实时反馈**: Python 端异步更新任务整体进度、TPS 及各账号独立进度至 Redis。
    - **前端可视化看板**: `TaskProgressCard` 组件重构，新增“分片进度条”与实时 TPS 监控面板，支持多账号执行进度可视化展示。

## 2026-01-18 (v2.3.5)

- **任务弹窗交互与布局深度优化**:
  - **校验逻辑精准修复**: 彻底解决打开任务弹窗时立即触发必填项（尤其是执行账号）报错的问题。通过为各表单项明确配置 `trigger: 'blur'` 或 `trigger: 'change'`，配合 `validation-trigger="blur"` 与 `clearValidate()`，确保弹窗初始状态纯净，仅在失焦或提交时触发校验。
  - **调度配置交互升级**: 新增“立即执行”与“不限时间”复选框，选中后自动禁用并清理对应的 Cron 表达式与持续时间，支持双向状态切换，大幅精简界面文案。
  - **动态布局自适应**: 深度调优 CSS，将报错文案强制设为相对定位 (`relative`)。现在当报错信息出现时，它会准确占据文档流空间，自动顶开下方元素，完全解决了 UI 堆叠覆盖 BUG。
- **任务编辑体验优化**:
  - **账号回显修复**: 修复了编辑任务时，执行账号在初始加载时显示为原始 ID（如 1,2）而非账号名称的问题。通过在弹窗打开时提前预加载账号列表，确保下拉框能够即时完成 ID 到名称的映射。
- **后端服务健壮性增强**:
  - **空指针异常 (NPE) 深度治理**: 针对 `/buff/account/list` 接口中由于 `Collectors.toMap` 遇到 null 值、缺失 `Schema` 导入以及 `BeanUtil` 反射实例化不确定性导致的 NPE 进行了彻底重构。现在采用防御性编程方式手动构建 Map，并强化了 DTO 的类型安全。

## 2026-01-18 (v2.3.4)

- **账号绑定机制升级**:
  - **唯一性限制**: 实现每个 BUFF 账号只能绑定一个扫描任务，防止多任务竞争同一账号导致风控风险。
  - **交互优化**: 任务配置弹窗中，已被其他任务占用的账号在下拉列表中将置灰显示，并标注占用的任务名称。
- **任务弹窗交互与布局优化**:
  - **校验逻辑修复**: 修复了打开任务弹窗时立即触发必填项报错的问题，改为仅在失焦或提交时校验。
  - **动态布局修复**: 修复了表单报错文案显示时与其他元素重叠的问题，报错信息现在会正常占据空间并顶开下方元素。

## 2026-01-18 (v2.3.3)

- **任务管理界面缺陷修复**:
  - **执行账号显示修复**: 修复了任务列表中“执行账号”列数据为空的问题，补全了前端表格的账号渲染模板。
  - **空状态友好化**: 对未绑定账号的任务增加了“未绑定”视觉提示。

## 2026-01-18 (v2.3.2)

- **Python 端启动兼容性与稳定性增强**:
  - **跨平台兼容性**: 修复了 `task_consumer.py` 在 Windows 系统下由于 `asyncio` 不支持 `add_signal_handler` 导致的崩溃问题，增加了平台检测与优雅降级。
  - **配置鲁棒性**: 在 `settings.py` 中补全了缺失的 `BUFF_COOKIE` 定义，修复了扫描器启动时的 `AttributeError`。
  - **环境自动初始化**: 优化了 `task_consumer.py` 的模块加载路径，确保在不同目录下运行均能正确识别项目配置。
  - **依赖补全**: 完善了 `httpx` 等核心异步请求库的安装校验。

## 2026-01-18 (v2.3.1)

- **任务配置界面缺陷修复**:
  - **间距修复**: 修复了“新增系统任务”与“新增任务”按钮紧贴在一起的问题，通过 Flex 布局增加了合理的间距。
  - **布局防堆叠**: 修复了系统任务对话框中由于单选框内容过长换行导致的元素堆叠问题，移除了硬编码的固定高度，允许表单项根据内容动态伸缩。
  - **选择器兼容性**: 优化了多选账号选择器的最小高度限制，确保在选择多个账号时布局不会错乱。

## 2026-01-18 (v2.3.0)

- **任务配置逻辑深度重构 (用户/系统任务分离)**:
  - **交互解耦**: 将“扫货任务”与“系统任务”彻底分离，新增独立的“新增系统任务”按钮及专属弹窗，解决了单一弹窗内容过多导致的 UI 兼容性与溢出问题。
  - **权限与类型隔离**: 仅管理员可见系统任务入口，弹窗内仅展示关联的任务类型（分类同步、商品同步、印花同步、分类商品同步），移除了用户任务无关的商品选择、磨损范围等字段。
  - **默认配置智能填充**: 针对系统任务引入了智能 Cron 推荐逻辑（如分类同步默认周日凌晨，商品同步默认每日凌晨），降低配置成本。
  - **逻辑复用与稳定性**: 保持了执行账号绑定、可视化 Cron 编辑等核心功能的共用，重构了编辑与提交逻辑，确保双弹窗模式下数据流转的准确性。

## 2026-01-18 (v2.2.1)

- **任务配置弹窗 UI 体验优化**:
  - **高度溢出修复**: 针对任务配置弹窗在编辑模式下内容过多导致超出屏幕的问题，通过极致压缩行高（mb-1.5）、减小内边距及优化组件间距，彻底解决了溢出问题。
  - **布局回退与兼容**: 撤销了导致横向滚动条的单行 Flex 布局，回归标准多行布局以确保 100% 宽度兼容性。
  - **视觉细节微调**: 统一表单项高度至 30px，精简对话框头部与底部内边距，确保在各种分辨率下均无滚动条。

## 2026-01-18 (v2.2.0)

- **账号绑定强制化与默认配置移除**:
  - **默认 Cookie 移除**: 彻底移除 `UserBuffSettings` 中的 `buff_cookie` 字段及系统环境变量 `BUFF_COOKIE`，消除全局回退路径，确保所有请求均基于明确的账号上下文。
  - **任务绑定强制校验**: 后端 `BuffScanTaskServiceImpl` 增加硬性校验，禁止创建或保存未绑定账号的任务；前端同步增加表单必填验证。
  - **爬虫模型同步**: 更新 Python 侧 `models.py`，新增 `BuffAccount` 与 `BuffScanTaskAccount` 模型，确保爬虫端与后端数据结构完全对齐。
  - **数据库结构演进**: 提供 SQL 脚本用于移除旧字段并建立多账号关联表及索引，优化查询性能。

## 2026-01-18 (v2.1.1)

- **BUFF 账号管理优化**:
  - **列表备注显示**: 在账号管理列表中新增“备注”列，支持显示用户自定义的账号备注信息，并提供 Tooltip 浮窗支持长文本查看。

## 2026-01-18 (v2.1.0)

- **多账号任务绑定系统实现**:
  - **任务账号关联 UI**: 在 `TaskConfig.vue` 中新增“执行账号”多选下拉框，支持为单个扫货任务绑定多个 BUFF 账号，实现多并发扫货。
  - **列表展示增强**: 任务列表新增“执行账号”列，实时展示任务绑定的账号名称，并对未绑定状态进行视觉提醒。
  - **类型系统对齐**: 更新 `BuffScanTask` 与 `TaskSaveParam` 类型定义，新增 `accountIds` 与 `accountNames` 字段，确保前后端数据流转一致。
  - **后端联动优化**: 验证并确保 `BuffScanTaskServiceImpl` 能够正确处理任务创建与更新时的账号关联保存逻辑（自动清理旧关联并批量插入新关联）。

## 2026-01-18 (v2.0.1)

- **编译稳定性与前端类型修复**:
  - **后端编译修复**: 补全 `BuffScanTaskServiceImpl` 缺失的 `LocalDateTime` 导入，修复 `@Slf4j` 日志注解缺失及 `RedisUtil` 方法名不匹配问题。
  - **前端类型对齐**: 完善 `BuffAccount` 接口定义，对齐 `PageResult` 响应结构，修复 `Dashboard` 表格对齐方式与按钮点击处理函数的类型冲突。
  - **交互逻辑加固**: 修正 `handleInputTrim` 在模板中的类型声明，确保自动清理逻辑在 `TaskConfig` 与 `StickerList` 等页面构建成功。
  - **冗余代码清理**: 移除 `Settings.vue` 中未使用的变量与样式函数，提升构建性能。

## 2026-01-18 (v2.0.0)

- **Niro 2.0 商业级多租户扫货引擎重构**:
  - **模型与分级队列**: 完成 Java 与 Python 侧多租户/多账号数据结构对齐，支持基于 Redis 的多级任务队列（系统级、扫货级、捡漏级）。
  - **自愈机制 (Self-Healing)**: 实现 `TaskMonitor` 守护进程，对比数据库状态与 Redis 心跳，实现 Redis 故障后的“无感恢复”。
  - **异步分布式消费者**: Python 引擎全面转向 `asyncio`，实现单任务多账号并行扫描、频率独立控制及 `contextvars` 状态隔离。
  - **分布式锁与原子下单**: 引入 Redis 分布式锁，确保多账号协同场景下同一饰品不被重复下单。
  - **全链路日志增强**: 强制在 Java MDC 与 Python Context 中注入 `userId`、`taskId`、`accountId`，支持 ELK 结构化检索。
  - **智行风格看板**: 前端 Dashboard 引入全链路状态进度条组件，实时展示“排队-扫描-匹配-下单”流程。

## 2026-01-16 (v1.22.2)

- **全链路追踪系统重构与去持久化**：
  - **TraceId 去持久化**：彻底移除数据库与实体类（Java/Python）中的 `traceId` 字段，确保业务逻辑与监控逻辑完全解耦。
  - **基于 Filter 的追踪实现**：引入 `TraceIdFilter` 替代拦截器，利用 Slf4j MDC 实现日志追踪，并强制在响应头注入 `X-Niro-Trace-Id`，确保异常情况下追踪 ID 不丢失。
  - **响应结构优化**：更新 `Result` 包装类，仅在接口报错时返回 `traceId` 供排查，成功请求仅通过响应头透传。
  - **Python 爬虫端同步**：利用 `ContextVar` 实现 Python 异步环境下的日志追踪 ID 自动流转。
  - **CORS 增强**：配置 `X-Niro-Trace-Id` 为暴露响应头，确保前端可正常读取。

## 2026-01-16 (v1.22.1)

- **后端编译与稳定性修复**：
  - **依赖版本规范化**：修复 `niro-server` 中 Elasticsearch 和 AMQP 依赖版本缺失问题，确保 Maven 构建稳定性。
  - **JDK 21 兼容性适配**：修复 `LogServiceImpl` 中 Stream 类型推导错误，适配 JDK 21 编译环境。

## 2026-01-16 (v1.22.0)

- **全链路日志与监控闭环重构**：
  - **Java 后端结构化改造**：引入 `logstash-logback-encoder`，实现 JSON 格式日志输出；集成全局 `TraceIdInterceptor`，在请求入口生成 UUID 并通过 MDC 传递，响应 Header 返回 `X-Niro-Trace-Id`。
  - **爬虫端全链路对齐**：支持从数据库任务中读取 `traceId` 并注入 Loguru 结构化日志；对齐时区与时间戳格式，确保 EFK 采集精准。
  - **EFK 采集链路优化**：重构 `filebeat.yml` 监控路径与动态索引命名（`niro-spider-*`/`niro-backend-*`），优化 `docker-compose` 卷挂载以共享日志文件。
  - **前端搜索增强与可视化**：重载 `Logs.vue` 日志页面，实现基于 `traceId` 的全链路日志搜索组件；引入日志视觉聚合机制，使用“成交绿” (#2ba471) 高亮下单成功日志，SSE 降级为轻量级心跳推送。

## 2026-01-15 (v1.21.0)

- **个人配置中心 UI/UX 深度演进**：
  - **布局固化与自适应**：重构为响应式 Grid 布局（7:3），引入 Aside 动态高度自适应机制，解决大屏换行与冗余滚动条问题。
  - **Aside 控制面板容器化**：将统计、配置、参数整合进统一的 `t-card` 容器，支持 Sticky 吸附定位。
  - **视觉精密化与降噪**：统一组件尺寸为 `small`，移除冗余背景与日志终端，优化板块间距与标题字号，提升专业工具感。
  - **账号管理弹窗重构**：缩减弹窗宽度至 `520px`，采用 Flex 响应式布局整合“账号名称”与“权重”行，美化 Cookie 输入框视觉深度，并精简内边距提升紧凑感。
  - **交互体验增强**：实现企业微信凭据默认折叠、余额显隐切换及全站输入框自动 `trim` 清理。

## 2026-01-15 (v1.20.0)

- **个人配置中心交互革命**：
  - **架构升级**：从“抽屉模式”全面升级为“常驻面板布局”，引入实时统计看板。
  - **全站表格规范化**：统一全站表格为 `embedded-card` 风格，建立列对齐与视觉盒模型规范。

## 2026-01-15 (v1.19.0)

- **账号管理体系深度闭环**：
  - **异步健康检测流**：实现账号创建后自动异步检测，引入实时状态联动。
  - **角色视觉重构**：改用 Ghost 风格标签与业务语义化色彩，优化信息优先级。
  - **后端参数映射修复**：解决 Spring Boot 3.x 环境下接口参数名获取导致的 500 错误。

## 2026-01-15 (v1.18.0)

- **BUFF 账号体系重构与视觉精细化**：
  - **多账号管理**：支持分布式角色分配（扫描/下单/全能）与风控熔断机制。
  - **视觉系统升级**：引入等宽数字字体、状态心跳动画及高价值资产高亮提醒。
  - **安全降级机制**：实现交易链接/API Key 异常时的角色自动降级与预警。
  - **稳定性修复**：补全数据库统计字段，修复 Controller 路径变量映射异常。

## 2026-01-15 (v1.17.0)

- **Buff 自动化测试增强**：支持 `count` 批量测试，引入 `TEST_XXX` 常量配置及 `argparse` 参数覆盖，提升开发回归效率。
- **爬虫端硬编码清理**：建立 `enums.buff_enums` 枚举体系，全面替换 `BuffSpider` 中的支付、排序、游戏类型等魔法值。
- **批量下单链路对齐**：深度还原官方 `预览->创建->支付预处理->状态轮询->最终购买` 标准闭环，实现 `TraceID` 全链路透传。
- **历史记录深度修复**：通过 Git 追溯恢复了从 v1.0.0 开始的所有迭代记录，并按日期进行简化合并，解决了历史记录丢失问题。

## 2026-01-14 (v1.16.45)

- **批量购买流程优化**：新增 `batch_buy_epay` 接口获取支付链接，支持非余额支付（支付宝/微信）场景下的“仅锁单”模式。
- **下单逻辑深度重构**：引入预创单撮合机制与风控预览补全，实现批量下单流程的原子化处理。
- **扫描与过滤增强**：支持无上限全场扫描及 Top 5 市场底价摘要打印，将精细化过滤逻辑（价格、磨损、支付方式）下沉至本地执行。
- **扫货精细化控制**：新增可用性校验，自动计算最大可买数量并自适应下单规模，深度对齐官方 API 冷却过滤等参数。
- **功能补全与修复**：新增分类商品即时同步功能（SYNC_CATEGORY_GOODS），修复后端 Service 冗余编译错误。

## 2026-01-13 (v1.16.33)

- **权限与数据安全**：实现任务管理系统的数据隔离与权限校验，普通用户仅能操作个人任务，管理员保留全局权限。
- **同步逻辑调优**：引入数据库实存校验解决数据漂移，强制 `price.asc` 排序提升分页稳定性，移除 Numpy 冗余依赖。
- **高级风控对抗**：引入正态分布随机延迟 (`smart_sleep`)、分段延迟策略及“长时休眠”模式，深度模拟真人行为特征。
- **分类抓取增强**：优化印花与特殊分类解析逻辑，支持全维度标签提取，重构 Redis 暂存机制以平衡内存与入库效率。
- **一致性与规范化**：新增数据一致性测试脚本，修复批量保存时的并发冲突，规范化 `niro-spider/tests/` 目录结构。

## 2026-01-12 (v1.16.23)

- **代理切换实测**：完成 Clash 自动切线功能验证，优化节点锁定逻辑，支持远程控制适配与智能配置推断。
- **智能重试策略**：引入指数退避与随机抖动 (Jitter) 策略，有效打散并发请求，降低二次触发限流风险。
- **风控深度集成**：将代理自动切换逻辑集成至爬虫核心重试回调，针对 429 限流与超时实现自动切 IP 的风控闭环。
- **ELK 日志采集与监控系统集成 (MVP)**：针对 2G 内存环境进行深度调优，禁用 ES 和 Kibana 高耗能插件，限制堆内存并配置容器级日志滚动策略。

## 2026-01-11 (v1.15.6)

- **扫描间隔时间范围功能**：支持最小/最大扫描间隔配置，并在指定范围内随机选择间隔时间，降低被风控风险。
- **前端交互 (UX) 增强**：重构任务配置页面，引入时间范围输入框及单位选择（秒/分钟/小时/天），优化执行摘要显示。
- **后端校验加固**：在 `BuffScanTaskServiceImpl` 中增强逻辑校验，确保扫描间隔设置的合法性。

## 2026-01-10 (v1.15.5)

- **爬虫系统自愈与刷新机制**：优化出口 IP 日志显示，增强同步刷新机制，引入重试自愈逻辑及系统任务扫描间隔校验解耦。
- **多级分类同步系统深度优化**：实现支持层级关系的 `Upsert` 逻辑，优化 `internal_name` 生成规则，提升单次同步覆盖率至 50 页。

## 2026-01-09 (v1.15.3)

- **爬虫日志系统闭环 (Unified Logging)**：实现入口对齐、格式一致性及双重 IP 验证，确保全链路追踪能力。
- **印花价值管理系统全面升级**：在 `buff_stickers` 表中新增在售数量字段，升级抓取模型支持多维数据同步，并优化 `upsert` 逻辑。
- **任务扫描频率安全加固**：统一扫描间隔下限为 15 秒，并同步更新前后端及爬虫端的默认值与兜底逻辑。

## 2026-01-08 (v1.14.3)

- **印花溢价捡漏核心引擎上线**：集成深度解析、估值模型及扫描器，支持印花元数据管理及饰品雷达指标（7天均价、流动性评分等）。
- **浏览器指纹管理架构重构**：新增 `BrowserProfile` 概念，内置主流浏览器指纹库，实现指纹生命周期管控与身份自动装填。
- **支付逻辑加固与模拟增强**：在部署前加固购买逻辑管控，完善模拟支付闭环与通知系统联动。

## 2026-01-06 (v1.10.1)

- **容器化环境深度适配**：彻底解决容器环境下日志时间偏差 8 小时问题，优化 Dockerfile 及系统级时区设置（Asia/Shanghai）。
- **数据库一致性与容错增强**：解决商品同步时的 `NotNullViolation` 约束冲突，补全 `tags` 字段映射，优化代理配置加载与反引号清洗。
- **爬虫 DTO 模型重构**：深度适配 Pydantic V2，利用 `AliasPath` 优化嵌套字段提取，精简 API 响应解析逻辑。

## 2026-01-05 (v1.8.8)

- **Python 爬虫架构现代化升级**：深度迁移 ORM 至 SQLAlchemy，引入 `Loguru`、`Pydantic`、`Tenacity` 等现代工具库，建立线程安全连接池。
- **网络与风控策略优化**：新增全局代理支持（v2rayA 适配）及代理开关控制，优化爬取间隔配置动态化，延长请求间隔至 10-15 秒。
- **系统任务重试与通知精简**：引入静默重试机制，减少通知干扰，并实现异常分级处理与企业微信告警联动。

## 2026-01-04 (v1.7.4)

- **Cookie 失效熔断与自愈机制**：引入“三连击”熔断策略，当任务连续 3 次检测到登录失效时自动停止并预警，支持状态语义化反馈（异常状态 3）。
- **全站跨域 (CORS) 与稳定性修复**：后端实现全局 CORS 配置，Nginx 代理层加固，前端优化 Axios 响应拦截与超时诊断。
- **Redis 基础设施统一管理**：建立单例 `RedisClient` 连接池，实现商品分类同步的 Redis 指纹校验与内存增量比对，大幅降低 DB 压力。
- **实时日志流 (SSE) 稳定性修复**：重构日志定位逻辑支持路径智能搜索，剥离 SSE 专用路由解决 Nginx 响应不闭合问题。

## 2025-12-27 (v1.3.16)

- **扫货任务调度系统重大升级**：引入 `APScheduler` 双层调度架构，支持秒级频率控制、Cron 定时触发及运行持续时间限制。
- **多用户爬虫实例隔离**：实现用户级 Cookie、请求头及风控状态隔离，支持个人化企业微信通知参数配置。
- **Cron 可视化编辑器上线**：基于 TDesign 实现 XXL-Job 风格的 Cron 配置界面，支持双向绑定、执行预览及 Quartz 规范校验。
- **全量商品抓取性能优化**：引入增量跳过逻辑与智能停机策略，效率提升 80% 以上，并增加指数退避限流算法。

## 2025-12-26 (v1.1.0)

- **项目架构解耦重构**：将 `niro-spider` 模块从 Java 服务中独立，实现彻底的语言栈解耦，并优化环境变量加载路径。

## 2025-12-25 (v1.0.18)

- **扫货逻辑与支付链路完善**：支持自动支付重试与降级（支付宝模式），实现“仅下单不支付”及企业微信实时通知。
- **Redis 连接稳定性增强**：优化 Lettuce 连接池配置，开启 TCP KeepAlive 与自动回收机制，解决连接超时异常。
- **个人配置中心上线**：实现 Buff Cookie 与支付方式的 Web 端可视化管理，引入高对比度卡片网格 UI 设计。

## 2025-12-24 (v1.0.1)

- **扫货任务管理系统 (MVP) 发布**：实现任务增删改查、状态控制及 SSE 实时日志流查看功能。
- **核心爬虫引擎建立**：实现基于价格与磨损范围的商品筛选逻辑，集成 CSRF Token 自动提取及模拟下单流程。
