- 每次回答问题时，需要从架构师和产品经理的角度去思考，业务是否符合实际需求？还有哪些操作或步骤可以优化？如何提升用户体验？如何提高系统性能？数据库设计是否合理？功能设计是否能满足当前的最小功能需求？如果用户提出的问题或逻辑在你思考后觉得有优化空间或者更好的建议，请不要直接改动或生成代码，而是回复建议，用户接受了再改动代码
- 每次思考时都必须读取所有的规则文件
- 所有的回答都需要先给出修改的内容后才执行代码修改
- **非常重要**：在根目录下的docs文件夹中有一个**release.md**文件，我需要你帮我维护一个项目迭代的记录，每次对代码有**大的改动**的时候需要简要记录改动内容，包括但不限于功能新增、功能优化、代码重构等，类似于github上的release，并且设置一个版本号，版本号的格式为v1.0.0，每次迭代后版本号需要递增，例如v1.0.1、v1.0.2等，如果是非常小的改动则不需要记录。
- 所有数据库的操作都给出sql语句，是否执行由我决定
- sql语句关键字必须小写，默认不使用双引号包裹
- 代码提交的描述必须使用中文
- sql语句需要完整的字段注释，所有的字段都需要默认值，并且需要添加必要的索引，重新给过sql语句
- release.md只需要在提交代码的时候更新一次，只需要简洁记录每次迭代的改动内容，版本号需要递增
- **release.md文件不需要记录代码文件位置，只需要简洁记录修改的内容或者功能的变动**
