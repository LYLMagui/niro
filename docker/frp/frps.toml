# frps.toml
bindPort = 7000

# 授权 Token
auth.token = "niro_frp_token_2024"

# 服务端 Dashboard 配置
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin123"
