services:
  frpc:
    image: snowdreamtech/frpc:nightly-alpine3.22
    container_name: frpc
    restart: always
    network_mode: host
    volumes:
      - ./frpc.toml:/etc/frp/frpc.toml
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
