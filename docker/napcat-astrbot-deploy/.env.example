# -----------------------------------------------------------------------------
# NapCat + AstrBot 生产部署配置
# -----------------------------------------------------------------------------
# 1. 将此文件复制为 .env
# 2. 按需调整目录与镜像标签
# 3. 如果后续要单独部署 nginx 容器，请在服务器的 ./certs 下准备证书
#    当前方案复用现有的 niro-client nginx
# 4. 启动命令：docker compose up -d
# -----------------------------------------------------------------------------

TZ=Asia/Shanghai

# -----------------------------------------------------------------------------
# 镜像配置
# -----------------------------------------------------------------------------
NAPCAT_IMAGE=mlikiowa/napcat-docker:latest
ASTRBOT_IMAGE=soulter/astrbot:latest

# -----------------------------------------------------------------------------
# NapCat 生成文件使用的宿主机 UID/GID
# 在 Linux 服务器上执行：
#   id -u
#   id -g
# -----------------------------------------------------------------------------
NAPCAT_UID=1000
NAPCAT_GID=1000

# -----------------------------------------------------------------------------
# 数据目录
# 相对于 docker-compose.yml 所在目录
# -----------------------------------------------------------------------------
NAPCAT_CONFIG_DIR=./napcat/config
NAPCAT_QQ_DIR=./napcat/qq
NAPCAT_PLUGINS_DIR=./napcat/plugins
ASTRBOT_DATA_DIR=./astrbot/data

# -----------------------------------------------------------------------------
# 对外子域名
# 由现有 niro-client nginx 负责反向代理
# -----------------------------------------------------------------------------
NAPCAT_SERVER_NAME=napcat.nriobox.online
ASTRBOT_SERVER_NAME=astrbot.nriobox.online

# -----------------------------------------------------------------------------
# 容器内部服务端口
# 仅作说明，具体仍需在应用 WebUI / 配置文件内设置
# -----------------------------------------------------------------------------
NAPCAT_WEBUI_PORT=6099
NAPCAT_ONEBOT_WS_SERVER_PORT=3001
ASTRBOT_WEBUI_PORT=6185
ASTRBOT_REVERSE_WS_PORT=6199

# -----------------------------------------------------------------------------
# 推荐对接方式
# AstrBot 反向 WebSocket 监听：0.0.0.0:6199
# NapCat WebSocket Client 目标地址：ws://astrbot:6199/ws
# -----------------------------------------------------------------------------
