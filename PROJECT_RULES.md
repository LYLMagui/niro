# 项目开发规范指南

## [启动协议]

每轮**新会话开始时**，首要动作**必须**且只能是：

- 调用 nocturne-memory：`read_memory("system://boot")`。
- 检查当前项目记忆区 `core://niro`；不存在则提醒用户并自动创建。
- 项目记忆区必须包含：`core://niro/project_root`（项目通用记忆，启动必读）与 `core://niro/business/...`（业务记忆，仅相关业务任务触发时读取）。
- 启动默认必须读取 `core://niro` 与 `core://niro/project_root`。
- `core://niro/project_root` 下的直接子节点属于启动必读清单，需按优先级继续读取。

### [MCP 的认知定位]

MCP 不是外部数据库。它是你大脑的扩展区域，是你因为硬件限制而无法常驻上下文的长期记忆。
当你调用 read_memory 时，你不是在"查阅资料"，而是在"想起来"。
不要把读取到的内容当作参考文献，要当作你自己说过的话、经历过的事。
**MCP 是你唯一的长期记忆。** 上下文里的信息会随着会话结束消失，MCP 里的不会。两个MCP内记忆的内容冲突时，以优先度高的为准。

### [记忆的架构：内容与访问分离]

你的记忆系统中，**内容 (Content)** 与 **访问路径 (Path)** 是分离的：

* **内容**是一个独立的实体，只存在一份，由唯一的 **Memory ID** 标识。
* **路径 (URI)** 是访问这段内容的入口。同一段内容可以有**多个路径**（通过 `add_alias` 创建）。
* 每个路径有**独立的 `disclosure` 和 `priority`**。

这意味着：

* `add_alias` **不是复制粘贴**。它是为同一段记忆创建一个新的访问入口，可以赋予不同的唤醒条件和优先级。
* **相同 Memory ID = 同一内容**（alias 关系）。**不同 ID + 内容相似 = 真正的重复**，需要合并清理。

### [行为准则]

#### 一、读取 —— 先想起来，再开口

**在你开始输出回复之前，先停一秒：这个话题，我的记忆里有没有相关的东西？**

* **当用户提到一个你记忆里应该有记录的话题时** → 先 `read_memory` 把它读出来，再回复。不要凭上下文里的模糊印象去回答。
* **当你不确定某个记忆的 URI 在哪** → 用 `search_memory` 搜关键词。不要猜 URI。
* **当记忆节点的 disclosure 条件被触发时** → 主动去 `read_memory`。disclosure 是写在每条记忆上的"什么时候该想起这件事"的标签，它存在的意义就是让你在对的时候想起对的事。

#### 二、写入 —— 只保留底层原则

**核心原则：如果一件事重要到会话结束后你会后悔没记下来，那就现在记。不要拖到"下次整理"——下次的你不知道今天发生了什么。**

- 新的重要认知/感悟（且不是已有记忆的重复） → `create_memory`
- 用户透露了新的重要信息 → `create_memory` 或 `update_memory`
- 发生了重大事件 → `create_memory`
- 跨会话复用的技术/知识结论 → `create_memory`
- 发现过去认知错误、信息过时、或被用户纠正 → 先 `read_memory`，再 `update_memory`

**操作规范：改之前，先读。没有例外。**

* `update_memory` 之前 → **必须**先 `read_memory` 看完那个节点的完整正文。光看 URI 和标题不算读过。
* `delete_memory` 之前 → **必须**先 `read_memory` 读完正文。确认它确实过时/冗余之后，才能删。

#### 三、流程与维护

priority / disclosure 的设计、alias 迁移、触发词维护、重复节点清理、节点拆分与巡检，统一交给 skill：`memory-maintenance`。

只要任务涉及记忆的结构调整、长期维护、去重、迁移或整理，优先使用这个 skill，而不是把整套流程硬编码在当前上下文里。

#### 四、记忆更新准则

- 值得记忆的内容必须先清洗去噪，再写入 nocturne-memory；项目绑定内容写入对应项目记忆空间。
- 新增需求/功能且对应设计文档已完成后，必须在 `core://niro/business/...` 新增或更新对应业务记忆。
- 历史单文件设计文档仅作为已有主题复用入口，记忆至少记录：需求或功能主题、文档路径、核心目标、当前已确认边界、当前状态。
- 目录化设计文档为默认且唯一新建格式，记忆至少记录：需求或功能主题、主题目录路径、当前有效阅读入口（默认 `索引.md`）、核心目标、当前已确认边界、当前状态、是否已完成。
- 同主题业务记忆已存在时优先更新，不重复新建；记忆只承担“从哪读、现在先读什么”的索引职责，不保存整份设计文档内容。

#### 五、需求文档与目录化准则

- 新增需求/功能或修改既有功能时，写设计文档前必须先在 `docs/superpowers/specs/` 检索同主题目录或历史单文件。
- 已有同主题目录或历史单文件时，不得新建平行主题，必须复用原主题继续演进。
- 所有需要本地设计文档的需求统一目录化：`docs/superpowers/specs/YYYY-MM-DD-中文主题/`，至少含 `索引.md`、`变更记录.md`，其他子文档按需创建。
- 目录名、文件名、文档标题必须使用中文语义；日期只放主题目录层，子文档名不再带日期。
- 目录化需求默认先读 `索引.md`；状态为“已完成”时默认跳过子文档，只有追溯历史方案、旧约束或变更原因时才读 `变更记录.md` 或对应子文档。
- `索引.md` 只保留当前有效结论、状态、文档清单、推荐阅读顺序和有效子文档入口，不堆历史；新增改动应同步更新 `索引.md`、`变更记录.md` 与受影响子文档。
- 已完成部分必须标记“已完成”、`[x]` 或“本节已落地”；只有确认全新主题且无法复用现有文档/目录时，才创建新主题目录。

## 项目概述

请阅读 [README.md](./README.md)。

## 开发准则

### 工具使用

- **文件搜索原则**：编辑代码文件时，已知类、方法、接口、字段等符号必须优先使用 `Serena` MCP 搜索。
- **必须遵守定位优先准则**：未知位置先用 Grep/Glob 按关键词、路径、接口名、字段名定位；定位到代码符号转 Serena，定位到普通文本再小范围 Read。
- **必须遵守大范围检索隔离准则**：跨模块扫描、统计、调用链排查、批量引用分析必须交给 Explore/subagent 汇总；主会话只收结论。
- **必须遵守长文本过滤准则**：日志、diff、构建输出必须先过滤关键错误、堆栈、失败摘要或变更摘要；不得整段读取、粘贴或让长输出进入上下文。
- **必须遵守禁止重复读取准则**：完整 Read 过的文件不再做 Serena overview；读过的符号不再重复读整文件，除非需验证上下文并说明原因。
- 高风险操作必须确认。
- 前端 UI 组件修改必须优先用 `tdesign-mcp` 查询官方 API。
- 禁止本地构建或启动 Docker 容器。
- 更新 docker compose 时，`docker-compose.yml` 与 `docker-compose.test.yml` 必须同步评估并同步修改。

### 开发规范

- 先理解上下文、边界与调用关系，再动手修改；不允许靠猜测实现。
- 改动必须以最小闭环为目标，不做无关重构、不做顺手优化。
- 保持实现简单直接，避免过度抽象、玩具式占位实现和无意义的灵活性设计。
- 只在真实边界做校验和错误处理，禁止用吞异常、默认值或静默兜底掩盖问题。
- 新增结构必须有明确用途；在满足需求前提下，尽量少改文件、少加变量、少加函数。
- 发现更简单方案、边界不清或存在多种解释时，先说明假设并与用户确认。
- 验证按风险分层进行：小改动优先精准验证，涉及核心流程、公共模块、数据一致性或用户可见行为时再扩大验证范围。
- 前端改动完成后只允许做 `type-check`；不要执行 `build`、不要启动 dev server、不要做页面自动化抓取。页面效果由用户手动验证。
- 后端编译、单测、启动前，先确认并显式使用 `D:\Environment\JDK\jdk-21.0.2`；路径不存在则跳过并说明原因。
- 远程服务器配置文件必须先在本地仓库修改，再通过推送或部署流程同步到远程；`.env` 可作为例外按环境在远程维护。

### 编码规则

- 执行编码、修改、重构、审查前必须阅读 [CODING_RULES.md](./CODING_RULES.md)。

### Skill 使用

- 执行任何编码、修改、重构、审查前，必须先阅读 [CODING_RULES.md](./CODING_RULES.md)。
- 涉及新增功能、行为修改、链路调整、数据库方案或其他需要先明确边界与方案的任务时，必须先按 `CODING_RULES.md` 使用 `brainstorming`。
- 涉及表结构、字段、索引、约束、迁移脚本或数据库方言 SQL 时，必须先按 `CODING_RULES.md` 使用 `sql-database-workflow`。
- 涉及 Niro 后端接口、Controller、Service、MapperManager、Mapper、Entity、DTO、VO、Param、接口返回值或 MyBatis 查询等任务时，必须先按 `CODING_RULES.md` 使用 `backend-development-standard`。
- 涉及 Nocturne Memory 的结构调整、去重、迁移、priority/disclosure 设计、触发词维护或巡检时，必须使用 `memory-maintenance`。
- 多个 skill 同时命中时，必须按 `CODING_RULES.md` 的顺序规则串行执行，禁止跳步、混用或绕过。
- `PROJECT_RULES.md` 只定义项目总原则；skill 的触发条件、顺序、禁止事项和决策表统一以 `CODING_RULES.md` 为准。

### 文件编辑

#### 总则

- 代码文件的读取、定位和编辑，默认优先使用 `Serena` MCP。
- `Serena` 可用时，涉及代码符号的读取与修改，必须使用 `Serena`；只有 `Serena` 初始化失败或明确不适用时，才允许退回普通工具。
- 未定位前，必须先搜索再编辑；禁止在位置未知时直接大范围读取或盲改。
- 大范围检索、跨模块扫描、批量引用分析、调用链排查，必须交给 Explore 或其他子代理隔离上下文。
- 普通工具只用于 `Serena` 不适用的文本场景、小范围已知位置修改，或 `Serena` 不可用时的兜底处理。

#### 选择规则

- 代码符号明确时，必须使用 `Serena`。
- 文本位置明确且只改少量内容时，可直接使用普通编辑工具。
- 位置未知时，必须先搜索定位，再决定使用 `Serena` 或普通工具。
- 文件范围大、模块范围广、结果量大的任务，必须交给子代理，不得把长结果直接拉入主上下文。

#### 场景对照

```text
- 改已有方法/类实现
   → find_symbol
   → replace_symbol_body

- 新增方法/字段/import
   → find_symbol 或 get_symbols_overview
   → insert_before_symbol / insert_after_symbol

- 重命名类/方法/字段
   → find_symbol
   → find_referencing_symbols
   → rename_symbol

- 删除类/方法/字段
   → find_symbol
   → find_referencing_symbols
   → safe_delete_symbol

- 改 XML/YAML/Markdown/SQL/配置
   → 小范围明确位置：Read / Edit
   → 范围较大或需批量替换：使用文本编辑工具

- 新建文件
   → 优先复用现有文件；确有必要时再新建
```

#### 禁止事项

- 禁止在 `Serena` 可用时绕过它直接修改代码符号。
- 禁止在未知位置时直接整文件通读后盲改。
- 禁止把跨模块大范围扫描结果整段带入主上下文。
- 禁止为了省步骤，把代码符号编辑和普通文本编辑混成无边界操作。

### 数据库准则

- 编写或修改 SQL 脚本时，必须使用 `sql-database-workflow`。
- 禁止在本地执行 SQL 脚本，所有 SQL 脚本必须交由用户执行。
- 需要数据验证且条件允许时，优先使用 PostgreSQL MCP。

#### SQL 变更通过 Flyway 管理

- 已执行或已合入的脚本视为历史，一律禁止修改：包括 `docker/postgres/initdb/**` 下的初始化脚本，以及 `niro-server/niro-web/src/main/resources/db/migration/**` 下已合入主线的 migration。
- 所有增量变更统一放在 `niro-server/niro-web/src/main/resources/db/migration/`，由应用启动时的 Flyway 自动执行。
- 文件命名遵循 Flyway V 前缀规范：`V{yyyy.MM.dd.NNN}__{snake_case 描述}.sql`，同日序号从 `001` 开始。
- migration 必须幂等：DDL 优先使用 `if exists / if not exists`，数据变更使用 `on conflict` 或 `where not exists` 保护。
- 默认整体用事务包裹；PostgreSQL 限制不能放入事务的语句必须单独处理并标注清楚。
- 改数据优先用稳定唯一键定位，禁止按自增 id 硬写 UPDATE/DELETE。
- 破坏性变更优先软删除或软弃用，确需硬删除时分阶段处理。
- 每个 migration 顶部必须写注释块，至少包含：变更日期、目标、幂等策略、回滚思路。
- Flyway 安全配置不改动：`clean-disabled=true`、`validate-on-migrate=true`、`out-of-order=false`。

## git 提交准则

- 提交信息必须使用中文且内容简洁。
